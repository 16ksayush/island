# Plan & Backlog — Archive 19: Dual-Atmosphere Dynamic Gallery

Status: **Phase 2 EXECUTION COMPLETE — M1–M7 done. M8 (Deploy): host CHOSEN — Render (D13); HostQ1 RESOLVED. Remaining open Qs = cold-start tolerance, domain, /api/refresh auth, Drive-sharing confirm, traffic/region (HostQ2–Q5 + R-D6) — shape but do not block the Render deploy (see M8 section). M9 Horror Atmosphere Redesign + landing map SHIPPED (2026-06-09). M10 Sea (Light) landing map (frontend-only) — BUILT + SECURITY-APPROVED + QA-VERIFIED, pending commit (2026-06-09) — see bottom section. After M9+M10 both themes are map-based. M11 Image captions — SHIPPED (156 passed), pending commit. M12 Build-time image baking — SHIPPED (182 passed), now SUPERSEDED by M13. **M13 Google Drive → Cloudinary image-source migration — BUILT + SECURITY-APPROVED + QA-VERIFIED (full suite 163 passed, hermetic) — pending commit (2026-06-09); see bottom section.** The image source is now **Cloudinary** (keyless public CDN); the Drive byte-proxy, the M12 build-bake, `app/drive_service.py`, and `scripts/fetch_images.py` are removed. M13 supersedes M12 and the Drive parts of M8 (which now needs only `CLOUDINARY_URL`).** Backend + frontend implemented, security-audited, full pytest suite green (**163 passed**, 0 failures, Cloudinary Admin API mocked), local `uvicorn` smoke test passing, deploy config (`render.yaml` + `Procfile`) committed. Remaining: provide `CLOUDINARY_URL`, then live verification on Render/Railway (M8). **M14 — Dynamic, atmospheric individual level/island pages — SHIPPED (2026-06-09): frontend-only redesign of `/level/{id}` (both themes, both states) replacing the flat dark/empty backdrop with the full-bleed decorated atmosphere the M9/M10 maps have. BUILT + SECURITY-APPROVED + QA-VERIFIED (full suite 185 passed, hermetic) + local `uvicorn` smoke PASSED — pending commit. New CSS/SVG classes: `level-env-*`, `level-ambient`, `sea-scene` (+ `sea-silhouette--*`, `<radialGradient id="sea-sun-lvl">` — the Sea LEVEL page now carries its own inline-SVG scene), `frame-corner--*`, `level-note`; `style.css?v=7`. `app/main.py` untouched (NO new SSR var — Q-M14-7). The existing `level.html` test contracts (HARD GATE, NF-M14-7) all survive; QA added `tests/test_m14_level_atmosphere.py` (21 tests). See REQUIREMENTS §16 / ARCHITECTURE §14 / the M14 section below.** **M16 — Chest-chooser gateway landing — BUILT + SECURITY-APPROVED + QA-VERIFIED (full suite 220 passed) + local `uvicorn` smoke PASSED — pending commit (2026-06-13). Phase 2 G3–G8 all DONE.** `GET /` is now a NEW theme-neutral gateway (`templates/gateway.html`, `<html data-theme-neutral>`, painted raster backdrop `static/img/gateway/gateway-bg.jpg` + transparent lowered cursive "choose a chest" caption + three symbol-only chests; ghost/cake zoom reveal → `Archive19Theme.choose()` cookie write → navigate); the two maps relocated to `/map/horror`+`/map/sea` (theme FORCED by route, D15); `/level/{id}` + `/api/*` unchanged; `style.css?v=9`; `tests/test_gateway.py` added (30 tests). As-built revision: D24's reveal art shipped as a budgeted raster backdrop (like the M9/M10 maps), not pure inline SVG. *(Discovery — Phase 1 — was signed off 2026-06-13: G0 (capture) + G1 (architecture, ARCHITECTURE §17, decisions D14–D24) + G2 (user sign-off).) The 3 BLOCKING Q's are answered: chest-2 = RANDOM 50/50 (D21); route names = `/map/horror`+`/map/sea` (user OVERRODE the architect's recommended bare `/horror`+`/sea`); show the gateway every visit. The 9 shaping Q's ride the architect defaults. `/` becomes a NEW theme-neutral gateway (cursive "choose a chest" + three chests; ghost→Horror / cake→Sea zoom reveal that sets the `theme` cookie before navigating); the maps relocate to `/map/horror`+`/map/sea` (forced theme, no redirect). First route/SSR change since M13 — `app/main.py` IS touched. Biggest risk = the test re-point off `/` (NF-M16-8) — concrete migration map in ARCHITECTURE §17.7, folded into G7. Next step: TD design (ui-designer) then Phase 2 build (G3–G8). See REQUIREMENTS §17 / ARCHITECTURE §17 / the M16 section below.**

## Milestones
- [x] **M0 — Discovery:** dual-theme PRD captured; requirements, architecture, plan documented; design questions answered.
- [x] **M1 — Scaffold & secure:** PRD file tree (`app/`, `static/`, `templates/`), `requirements.txt`, `.env.example` (`GD_API_KEY`, `GD_ROOT_FOLDER`), hardened `.gitignore`, audio tree `static/audio/{global,horror,sea}/` (missing-level fallback audio is from Drive `missing/`, not local — D6/D9).
- [x] **M2 — Backend dynamic core:** `drive_service.py` (Drive client under `GD_ROOT_FOLDER`, child discovery for dynamic scaling, `missing/` image fallback), `main.py` routes incl. theme cookie.
- [x] **M3 — Theme system:** `style.css` `.theme-horror`/`.theme-sea`, global toggle on every page, cookie + sessionStorage persistence, SSR theme read.
- [x] **M4 — Landing grid:** dynamic Corridor Grid ⇄ Island Map; renders configured levels + missing tiles.
- [x] **M5 — Level pages:** `/level/{id}` (`level.html`) room/beach presentation; image rendering via proxy.
- [x] **M6 — Audio engine:** global ambient per theme + per-level crossfade, auto-play-safe.
- [x] **M7 — Harden, test & verify:** error handling, missing-folder paths, security audit per file (T6/T11 APPROVED), **QA_TESTER pytest suite (Drive mocked) + behavioral checks — 58 passed, 0 warnings**, local `uvicorn` smoke test (`/`, `/api/levels`, theme cookie all 200; graceful degradation with no GD env). All units frozen post-audit + green tests.
- [ ] **M8 — Deploy:** Render/Railway config committed (`render.yaml` Blueprint + `Procfile`); **live verification pending**. *(After M13: needs only `CLOUDINARY_URL` set in the host dashboard — no Google Drive vars, no build-time bake; build is just `pip install`.)*

> **Two-layer `available` semantics (QA-flagged, by design):** `/api/levels` reports `available` = the numbered Drive folder *exists*; `/api/levels/{id}/photos` reports `available` = the folder exists **and** is non-empty. A level can be listed as available yet still serve the `missing/` fallback when its photos are fetched.

## Sequenced backlog (Phase 2 — `orchestrate-build` after sign-off)
| # | Task | Owner | Depends on |
|---|---|---|---|
| T1 | Scaffold PRD dir tree + `requirements.txt` (fastapi, uvicorn, jinja2, httpx) | project-manager | M0 |
| T2 | Hardened `.gitignore` + `.env.example` (`GD_API_KEY`, `GD_ROOT_FOLDER`) | security-engineer | T1 |
| T3 | `GD_ROOT_FOLDER` parent link config (placeholder in `.env.example`) + audio tree layout | solutions-architect | T1 |
| T4 | `drive_service.py`: Drive client under parent folder, child-discovery scaling, `missing/` image+audio fallback. **AC: missing-level fallback re-rolls a fresh random image+audio from `missing/` on every `/photos` call (R3); builds `folder_index`/`missing_folder_id`/`levels` once and caches, refreshed only via `POST /api/refresh` (R2).** | backend-engineer | T3 |
| T5 | `main.py`: `/`, `/level/{id}`, `/api/levels`, `/api/levels/{id}/photos`, `/api/.../media/...`, theme cookie. **AC: media proxy echoes upstream Drive `Content-Type` (image/* vs audio/mpeg), never hardcoded (R6); empty-but-present numbered folder falls through to missing fallback (R5, default); discovery is startup-built + cached with an optional manual `POST /api/refresh` (R2, default); Drive `files.list` handles `pageToken` defensively (R4).** | backend-engineer | T4 |
| T6 | Security audit of backend files (key isolation, missing-path safety). **AC (hard, veto): media proxy validates `file_id` ∈ the resolved level folder (or `missing/`) before streaming — reject unknown ids with 404, no open-proxy for arbitrary public Drive files (R1).** | security-engineer | T5 |
| T6b | **Backend test suite** (pytest + TestClient, Drive mocked): routes, dynamic discovery, missing fallback, theme cookie, error handling. **AC: assert media proxy rejects a `file_id` not in the level/`missing/` folder → 404 (R1); asserts proxy response `Content-Type` mirrors the mocked upstream (R6); asserts missing-level fallback may vary across `/photos` calls and always returns a valid image+audio pair drawn from the mocked `missing/` set (R3).** | qa-tester | T6 |
| TD | **Design phase** (`design-webpage` skill → `ui-designer`): `design/DESIGN_SYSTEM.md`, `design/tokens.css`, static mockups `landing-{horror,sea}.html` + `level-{horror,sea}.html` w/ working theme toggle; user look/feel sign-off. **Now a standing step: all future UI work (M9+ redesigns) routes through here before the frontend-engineer implements.** | ui-designer | T3 |
| T7 | `style.css` dual-theme classes + global theme toggle (cookie + sessionStorage) — adopts `design/tokens.css` | frontend-engineer | TD, T5 |
| T8 | `index.html` dynamic Corridor/Island grid | frontend-engineer | T7 |
| T9 | `level.html` room/beach level page + proxied images | frontend-engineer | T7, T5 |
| T10 | Audio crossfade engine (global ⇄ level), auto-play-safe | frontend-engineer | T8, T9 |
| T11 | Security audit of frontend files | security-engineer | T10 |
| T11b | **Frontend/behavioral tests**: SSR theme class per cookie, grid renders configured levels + missing tiles, route smoke checks | qa-tester | T11 |
| T12 | Full suite run + local `uvicorn` smoke test + missing-folder + theme-toggle verification | project-manager | T6b, T11b |
| T13 | Render/Railway deploy config + instructions | project-manager | T12 |

## Gate
✅ **Discovery complete** and ✅ **Phase 2 execution complete.** All units built, audited (T6/T11), and verified (58 tests green + uvicorn smoke). Built with placeholders (`GD_ROOT_FOLDER` + audio stubs filled in locally later). Drive shared public (`GD_API_KEY`); `missing/` folder holds stock images + audio. Remaining gate: **M8 live deploy** — user supplies real credentials/assets, then verify on Render/Railway.

### Risk triage (architect-surfaced)
**Locked by default (no user input required; defaults recommended):**
- R1 — Media proxy MUST validate `file_id` ∈ resolved level/`missing/` folder before streaming; unknown → 404. Hard security AC on T6 (SECURITY_ENGINEER veto).
- R6 — Media proxy echoes upstream Drive `Content-Type`; never hardcodes `image/*`. Hard backend AC on T5.
- R4 — `files.list` handles `pageToken` defensively (loop until no token). Low risk; AC on T5.
- R5 — Empty-but-present numbered folder (zero images) → falls through to missing fallback (treated as not-yet-stocked). AC on T5.
- R2 — ✅ **DECIDED (Q1):** Discovery is startup-built + cached, with a manual `POST /api/refresh` to re-list without a redeploy. No automatic TTL.
- R3 — ✅ **DECIDED (Q2):** Missing-level fallback is **re-rolled on every `/photos` request** — a fresh random image + audio from `missing/` each visit, for a shifting/uncanny effect. No per-level seeding.

**Clarifying questions — RESOLVED:**
1. (R2) Discovery freshness → startup cache + manual `POST /api/refresh` (no automatic TTL).
2. (R3) Missing-level look → re-rolled on each visit (shifting/uncanny), not stable per level.

---

## Horror Atmosphere Redesign (change request — 2026-06-08)

Status: **Discovery COMPLETE — design approved (ARCHITECTURE §8), decisions LOCKED, ready for `orchestrate-build`.** Frontend-only visual restyle of the HORROR theme (REQUIREMENTS §9, HR1–HR8 / NF-HR1–NF-HR6). Sea & Island, backend, routes, payloads, audio, and deploy config are all out of scope and unchanged. Architect surfaced 6 technical risks (R-H1…R-H6, ARCHITECTURE §8.9) — all folded into the backlog ACs below.

**User-locked decisions (2026-06-08):**
- HQ1 — Background technique: **pure CSS/SVG** (no raster asset; H5 skipped).
- HQ2 / R-H1 — Cloaked hooded figure: **INCLUDED** (user override of the OFF default) — must be tuned (low opacity, anchored low-center) so it does not clutter or sit under the door grid.
- HQ3 / R-H6 — Per-level page intensity: **FULL corridor scene** behind the slideshow (user override of the calmer default) — so R-H5 glow-wash guard + R-H2 contrast check over the slideshow are now critical, not optional.
- HQ4 — Ambient motion: **ON, subtle**, frozen under `prefers-reduced-motion`.

### Milestone
> **⚠️ Shipped design (2026-06-09) diverged from the locks above.** HQ1 ("pure CSS/SVG, no raster asset") and HR3 ("keep the door grid, restyle only") were **superseded**: the Horror landing now uses a full-bleed **raster illustrated map** (`static/img/horror/landing-map.v2.jpg`) that *replaces* the door grid, with %-positioned clickable hotspots → `/level/{id}` (`?calibrate` to tune them). Drifting ghosts/silhouettes/fog and the full per-level corridor scene (HQ2/HQ3/HQ4) shipped as planned. Sea theme untouched. See REQUIREMENTS §9.3 note and ARCHITECTURE §8.3 note.

- [x] **M9 — Horror Atmosphere Redesign + landing map (frontend-only):** Cinematic haunted-corridor background on the Horror landing and a horror ambiance behind each per-level stage, via the existing `.atmosphere` overlay + `.theme-horror` `--page-bg-image`/`--vignette` injection points. Door grid layout, all functionality, performance (prefetch/cache), accessibility (`prefers-reduced-motion`), responsiveness, and theme isolation (Sea untouched) all preserved. Security-audited + QA-regression-verified before freeze.

### Sequenced backlog (M9 — after Open Questions resolved)
| # | Task | Owner | Depends on |
|---|---|---|---|
| H0 | Resolve Open Questions HQ1–HQ5 (HQ1 asset strategy, HQ2 decorative scope, HQ3 per-level intensity, HQ4 motion, HQ5 figure/claw page-scope) + architect risk decisions R-H1 (figure on/off) and R-H6 (per-level intensity). All carry recommended defaults (ARCHITECTURE §8.8) — a "go" locks them; only the user-facing subset (see PM Discovery questions) needs an explicit answer. | project-manager + user | §9 |
| H1 | Design technical approach: CSS/SVG vs image-asset vs hybrid; exact `.theme-horror` token changes (navy palette, moonlit `--vignette`/`--page-bg-image`), `.atmosphere` layering plan, responsive `background-size`/position, per-level treatment (HR2). Confirm zero impact to Sea + JS/payloads. **DONE — see ARCHITECTURE §8.1–§8.8.** | solutions-architect | H0 |
| H2 | Implement Horror landing background — restyle `.theme-horror` tokens + atmosphere in `static/style.css` + horror-scoped decorative SVG in `index.html`. Preserve door-grid markup + tile contract (HR3/HR5). **AC (R-H1, LOCKED ON):** the cloaked hooded figure IS included — render it as a faint, low-opacity inline-SVG silhouette anchored low-center behind the grid, `aria-hidden`, `pointer-events:none`; tune opacity/placement so it never obscures or sits under a clickable door tile. Moonlit doorway + faint edge clawed-hands + fog also included. | frontend-engineer | H1 |
| H3 | Implement Horror per-level background — `.theme-horror .level-stage`/page background (HR2); keep slideshow, nav, dots, contrast (HR6) intact. **AC (R-H6, LOCKED — FULL scene):** per-level uses the full corridor scene (glow + fog + figure/claws), NOT the calmer variant. **AC (R-H5, now critical):** because the full scene sits behind the slideshow, keep the `.level-stage` inset shadow AND add a backing scrim/panel behind the images so the central glow + fog never wash out slideshow edges or reduce photo legibility. | frontend-engineer | H1 |
| H4 | Apply chosen decorative elements (moonlit door, figure, clawed hands, fog) per HQ2, and optional reduced-motion-gated ambient motion per HQ4 (HR7/HR8, NF-HR2). **AC (R-H3):** moonlight flicker stays slow/subtle (no rapid luminance flashing, well under 3 flashes/sec) and freezes to a static frame under `prefers-reduced-motion` via an explicit fallback (not the global kill-switch alone). **AC (R-H4):** fog `filter: blur()` kept to small fixed layers; if mobile profiling shows cost, reduce blur radius or bake into the gradient. | frontend-engineer | H2, H3 |
| H5 | (If HQ1 = image/hybrid) Add + optimize background asset(s) under `static/img/horror/`, document source/license; verify byte budget ≤150 KB WebP (NF-HR1). **Skipped under the recommended pure-CSS/SVG default.** | frontend-engineer | H1 |
| H6 | Security audit: confirm diff is restricted to horror-scoped CSS/templates (+ any `static/` asset), no secret/Drive exposure, no open inline-script changes, decorative layers `aria-hidden`. | security-engineer | H2, H3, H4, H5 |
| H7 | QA regression: SSR renders `.theme-horror` from cookie unchanged; **Sea theme byte-unchanged / visually unaffected**; grid renders configured + missing tiles; toggle/nav/slideshow/audio still work; `prefers-reduced-motion` honored; responsive at mobile/desktop; existing pytest suite still green (58). **AC (R-H2):** run a WCAG AA contrast check on `--fg`/`--muted`/`--accent` and slide dots against the new navy `--bg`+fog before freeze; bump `--muted` lightness if it fails. **AC (R-H4):** mobile QA pass confirming no fog-blur jank on a low-end/mobile GPU. | qa-tester | H6 |
| H8 | Local `uvicorn` smoke test (`/`, `/level/{id}`, theme cookie both themes), visual sign-off, confirm no perf regression (prefetch/cache intact). **AC (R-H1):** visual eyeball of the figure decision before sign-off; **AC (R-H5):** confirm level-page glow does not wash the slideshow. | project-manager | H7 |

### Gate (M9)
✅ **Discovery + design complete; H1 done (ARCHITECTURE §8); H0 decisions LOCKED by the user (see status block).** All 6 architect risks (R-H1…R-H6) folded into H2/H3/H4/H7/H8 as acceptance criteria. **Released for `orchestrate-build` Phase 2** (FRONTEND_ENGINEER H2/H3/H4 → SECURITY_ENGINEER H6 → QA_TESTER H7 → PM H8). User chose: pure CSS/SVG, figure INCLUDED, FULL per-level scene, subtle motion. No deploy-config or backend changes; M8 (live deploy) status is independent.

---

## M10 — Sea (Light) landing map (change request — 2026-06-09)

Status: **Discovery COMPLETE — requirements captured (REQUIREMENTS §10, SR1–SR9 / NF-SR1–NF-SR6), technical design AUTHORED (ARCHITECTURE §9.1–§9.9), architect defaults LOCKED, USER SIGN-OFF received (2026-06-09): art used as-is for the Sea/light theme, names in aria-label, no mobile grid fallback. Released for `orchestrate-build` Phase 2.** Frontend-only redesign of the SEA (light) theme landing: replace the dynamic island grid with a full-bleed illustrated archipelago map (`static/img/light/landing-map.png`, optimized to `landing-map.v2.webp` ≤400 KB before ship) + 19 %-positioned hotspots → `/level/{id}`, mirroring the shipped M9 Horror map. Horror theme, backend, routes, payloads, audio, and deploy config are out of scope and unchanged.

### Milestone
- [x] **M10 — Sea (Light) landing map (frontend-only) — BUILT + SECURITY-APPROVED + QA-VERIFIED (full suite 140 passed / 0 failed); pending commit (PM S8 done):** Full-bleed illustrated archipelago map on the Sea landing replacing the dynamic island grid, with 19 calibrated %-positioned clickable hotspots → `/level/{id}` and sealed/sunken styling for unavailable levels. Theme toggle overlaid (clear of the painted pill), keyboard-accessible hotspots with name-aware aria-labels, optimized image (`landing-map.v2.webp`, 214 KB) within byte budget, `prefers-reduced-motion` honored, responsive mobile→desktop. Horror landing byte-unchanged; security-audited + QA-regression-verified.

### Open Questions — resolution state (full text in REQUIREMENTS §10.5 / ARCHITECTURE §9.8)
**Architect-LOCKED-on-go (no user input needed):**
- **SQ1** class strategy → reuse shared `.map-hotspot*` leaf classes both themes + Sea-scoped `.sea-map*` containers; NO macro, NO Horror rename (§9.2).
- **SQ2** image → single WebP `landing-map.v2.webp`, q≈80, ~1600px, ≤400 KB (ceiling 600 KB) (§9.4).
- **SQ4** ambient → off by default; `.sea-ambient` hook documented (§9.6).
- **SQ5 (scale/pan model)** → scale-to-fit + letterbox desktop, pan-to-fit mobile, no grid fallback (§9.5).

**RESOLVED by user sign-off (2026-06-09):**
- **SQ6** → ✅ art used for the Sea & Island (light) theme **as-is**; Sea keeps its sunlit azure palette + "Enter the Corridor"/🌙 copy. Painted location names treated as island names, not a horror restyle.
- **SQ3** → ✅ names exposed in `aria-label` only ("Level 5 — Prison"); visible UI numbers-only.
- **SQ5 sub-question** → ✅ NO mobile island-grid fallback; map scales/pans to fit on all sizes; grid markup deleted.

### Sequenced backlog (M10 — decisions LOCKED; tasks reflect ARCHITECTURE §9)
| # | Task | Owner | Depends on |
|---|---|---|---|
| S0 | **Sign-off gate.** Architect defaults are LOCKED (SQ1/SQ2/SQ4/SQ5-model). Obtain the explicit USER answer to **SQ6** (art-for-Sea as-is + azure palette/copy kept + eerie names in aria vs suppressed → resolves SQ3) and the **SQ5 sub-question** (mobile grid fallback? default NO). A simple "go with defaults" satisfies all of these. R-S1/R-S4/R-S6 closed-or-mitigated; R-S2/R-S3/R-S5/R-S7/R-S8 carried as ACs below. **✅ DONE — user sign-off 2026-06-09 (art as-is, names in aria-label, no mobile grid fallback).** | project-manager + user | §10, §9 |
| S1 | **Technical design — DONE (ARCHITECTURE §9.1–§9.9).** Class strategy (§9.2 SQ1), `sea_coords`+`sea_names` source (§9.3), image opt spec (§9.4 SQ2), responsive plan (§9.5 SQ5), ambient hook (§9.6 SQ4), theme-isolation proof (§9.7 NF-SR4), defaults (§9.8), risks R-S1…R-S8 (§9.9). | solutions-architect | S0 |
| S2 | **✅ DONE.** **Produce the optimized map asset:** `static/img/light/landing-map.v2.webp` from the 2.7 MB `landing-map.png` — WebP q≈82, **1399×768**, **214 KB** (under the ≤400 KB budget); preserve native aspect ratio and **record the exact final WxH** (§9.5 responsive math needs the true ratio). Do NOT ship the raw PNG (retain it only as the design source). Document source/license + optimization origin (R-S2/R-S7, NF-SR1/NF-SR6). | frontend-engineer | S1 |
| S3 | **✅ DONE.** **Sea branch markup** — replaced ONLY the body of the `{% else %}` (Sea) arm in `index.html` (lines ~116–171) with the §9.2 skeleton: `.sea-landing` > `.sea-map` > `.sea-map-img` (`src=".../landing-map.v2.webp"`, `fetchpriority="high"` `decoding="async"`) + `.sea-map-hotspots` looping the inline **`sea_coords`** dict (§9.3) into **shared `.map-hotspot`** anchors (`.map-hotspot-ring`/`.map-hotspot-id`, `.is-sealed` for id ∉ `available_ids`) → `/level/{id}`; `sr-only` `<h1>`; `.sea-map-toggle` overlay. Add the inline `sea_names` dict for aria-labels. **Plus the one-line calibrate-JS change** `document.querySelector(".horror-map")` → `.querySelector(".horror-map, .sea-map")` (§9.2). **Horror arm + shared `.atmosphere` block left byte-identical.** **AC (SQ5 sub, RESOLVED):** delete the Sea grid markup — no mobile grid fallback (user sign-off 2026-06-09). | frontend-engineer | S1, S2 |
| S4 | **✅ DONE.** **Sea map CSS** — NEW block of `.theme-sea .sea-map* { … }` rules in `static/style.css` (parallel to the M9 Horror block): container/img scale-to-fit + letterbox via a Sea-palette `.sea-landing::before` blurred fill (light sky/sand wash, not Horror's storm wash); **shared `.map-hotspot` ring/id/focus styled under `.theme-sea`**; **sealed/"sunken"** styling for unavailable levels (SR5); a Sea-scoped **calibrate twin** (`.theme-sea .sea-map.calibrate …`); Sea mobile breakpoints (pan-to-fit: `.sea-landing` fixed/scroll, `.sea-map-img` 130vw ≤768 / 175vw ≤480, `.map-hotspot` 7% ≤480). **AC (NF-SR4):** every rule prefixed `.theme-sea`; no bare `.map-hotspot{}`; no edit to `.horror-*`, `:root`, shared `html,body`/`.atmosphere`/`.layer`/`.theme-toggle` base. | frontend-engineer | S1 |
| S5 | **✅ DONE.** **Accessibility + toggle + (deferred) decoration** — keyboard-focusable hotspots with name-aware `aria-label`s "Level {id} — {name}" (SR7, SQ3 default), `(sunken — fallback content)` suffix for sealed; decorative layers (`.sea-map-img`, painted-pill region, any ambient) `aria-hidden`+`pointer-events:none`; `.sea-map-toggle` overlaid top-right OVER the painted pill (🌙 "Enter the Corridor", SR6, R-S3). Ambient `.sea-ambient` stays **OFF** (SQ4 default) — hook only. | frontend-engineer | S3, S4 |
| S6 | **✅ DONE — APPROVED.** **Security audit** — confirmed the diff surface is **exactly** {Sea `{% else %}` branch of `index.html` + the one-line calibrate-JS selector, the new `.theme-sea .sea-map*` CSS block, the new `static/img/light/landing-map.v2.webp`} and nothing else; no secret/Drive exposure; no open inline-script changes beyond the documented one-line selector; decorative layers `aria-hidden`; **committed image ≤400 KB (ceiling 600 KB)** (R-S2). Veto on any out-of-scope edit. | security-engineer | S3, S4, S5 |
| S7 | **QA regression** — SSR renders `.theme-sea` map from cookie; **Horror landing byte-unchanged / visually unaffected and Horror `?calibrate` still highlights `.horror-map` (R-S1)**; all 19 hotspots navigate to `/level/{id}`; sealed styling for unavailable ids; **all 19 hotspots reachable on MOBILE incl. bottom-row 12/15/17 via pan-to-fit (R-S8)** and hotspots clear ~44px (R-S5); toggle overlay lands on the painted pill without looking duplicated (R-S3); toggle/nav/slideshow/audio/prefetch still work; `prefers-reduced-motion` honored; WCAG AA contrast on hotspot rings/ids/focus over the bright sunlit art (NF-SR2); committed asset within budget (R-S2); existing pytest suite still green. | qa-tester | S6 |
| S7 | **✅ DONE — VERIFIED.** Added `tests/test_sea_landing.py` (40 tests); reconciled 5 stale grid tests in `tests/test_frontend.py` + `tests/test_horror_atmosphere.py` to the map model. **Full suite 140 passed / 0 failed.** (Row above is the original S7 spec.) | qa-tester | S6 |
| S8 | **✅ DONE.** Completeness check (full suite 140 passed / 0 failed), README + docs synced to the map model (this commit), commit-readiness report produced. No backend/route/payload/audio/deploy change → M8 (Render/Railway) unaffected. Commit is the user's call. | project-manager | S7 |

### Gate (M10)
✅ **BUILT + SECURITY-APPROVED (S6) + QA-VERIFIED (S7, full suite 140 passed / 0 failed) + PM doc-sync done (S8) — pending commit (user's call).** Phase 2 ran end to end: FRONTEND_ENGINEER S2/S3/S4/S5 → SECURITY_ENGINEER S6 → QA_TESTER S7 → PM S8. Risks: R-S1 mitigated (Horror `?calibrate` still highlights `.horror-map`, Horror landing byte-unchanged), R-S2 closed (asset 214 KB ≤ 400 KB budget), R-S4/R-S6 closed. No backend or deploy-config changes; M8 (live deploy) status is independent and unaffected. After M9+M10 both themes are map-based.

---

## M8 — Deploy (free-tier hosting) — Discovery (change request 2026-06-09)

Status: **Host CHOSEN — Render (D13, user-decided 2026-06-09); HostQ1 RESOLVED.** The architect's host-fit matrix (ARCHITECTURE §10) verdict — **Render primary**, Fly.io / HF Spaces fallbacks, Vercel/Pages rejected — was accepted. Remaining open Qs = cold-start tolerance (HostQ2), domain (HostQ3), Drive-sharing confirm (HostQ4), traffic/region (HostQ5), and `/api/refresh` auth (R-D6) — these shape the Render deploy but do **not** block starting it. Trigger (verbatim): *"how are we going to host the website? Need a free hosting tool, maybe github / vercel? ask question if you have any."* This is the **last open milestone**. Build is complete and green (M1–M7 done; M9/M10 shipped; full suite 140 passed — CI green and must stay green, HostR10). Deploy config already committed: `render.yaml` (Render free web service Blueprint) + `Procfile` (kept as a Fly/Heroku-style portability artifact). What remains is the **Render deploy** + live verification — no feature code.

### Milestone
- [ ] **M8 — Deploy (Render, D13):** apply the committed `render.yaml` Blueprint as a Render free web service (satisfies HostR1–HostR11 — persistent Python ASGI server, server-side `GD_API_KEY`, public HTTPS, auto-deploy from `main`, `/api/levels` health check, $0); set `GD_API_KEY` (secret) + `GD_ROOT_FOLDER` (config) in the Render dashboard; deploy from `main`; live-verify both themes + `/level/{id}` (missing + available) + theme toggle + real Drive content + `POST /api/refresh`; document the deploy runbook in `README.md`. **Host chosen; remaining open Qs HostQ2–Q5 + R-D6 shape the deploy but do not block it.**

### Host-class summary (full analysis → ARCHITECTURE, next)
- **GitHub Pages** — static-only → **NOT viable** (fails HostR1/R2/R3; HostC1).
- **Vercel** — serverless; FastAPI runs but the startup-discovery + in-memory cache + `POST /api/refresh` model is stateless/ephemeral → **rework + tradeoffs** (HostC2).
- **Render** (free web service) — **already Blueprinted**; persistent; idle spin-down ~15 min → cold start (HostC3, HostQ2). **Lean default for HostQ1.**
- **Fly.io / Hugging Face Spaces** — persistent free options; would need a `Dockerfile`/`fly.toml` (HostC3).
- **Railway** — `Procfile` committed but **no longer truly free always-on** (HostC4) → fallback/portability artifact, not the HostR7 answer.

### Sequenced backlog (M8 — Render path; host chosen D13)
Legend: **[USER]** = action only the user can take (account, real secret, Drive setting); **[AUTO]** = automatable / PM- or platform-driven.
| # | Task | Owner | Depends on |
|---|---|---|---|
| D0 | **✅ DONE — Host requirements + open questions captured** (REQUIREMENTS §12). | project-manager | M7, §12 |
| D1 | **✅ DONE — Host-fit decision matrix** (ARCHITECTURE §10): Render primary, Fly.io / HF Spaces fallbacks, Vercel/Pages rejected. | solutions-architect | D0 |
| D2 | **✅ DONE — HostQ1 RESOLVED → Render (D13, user-decided 2026-06-09).** HostQ2–Q5 + R-D6 carried as shaping (non-blocking) questions in the Discovery Report. | project-manager + user | D1 |
| D3 | **[USER] Confirm Drive precondition + real credentials ready** — Drive parent stays **"Anyone with link → Viewer"** (HostQ4 / D12 — hard runtime precondition, else every fetch 502s); real `GD_API_KEY` + `GD_ROOT_FOLDER` values in hand. Confirm CI is green on `main` (HostR10) and stays green. | project-manager + user | D2 |
| D4 | **[USER] Apply the `render.yaml` Blueprint** — in the Render dashboard: **New + → Blueprint**, point at this GitHub repo on `main`; Render reads the committed `render.yaml` (no new code/config). Requires a (free) Render account connected to GitHub. | project-manager (guides) + user | D3 |
| D5 | **[USER] Set env vars in the Render dashboard** — `GD_API_KEY` as a **secret** and `GD_ROOT_FOLDER` as config (both `sync:false` → Render prompts). Never committed (HostR2/HostR3). Pick region per HostQ5; subdomain vs custom domain per HostQ3. | project-manager (guides) + user | D4 |
| D6 | **[AUTO] First deploy + health check** — Render auto-builds from `main` (`pip install -r requirements.txt` on Python 3.13, HostR8) and runs `uvicorn app.main:app`; `lifespan` discovery builds the cache; Render polls **`/api/levels`** until healthy (HostR5/HostR6). Auto on push thereafter. | project-manager (+ render) | D5 |
| D7 | **[AUTO+USER] Live verification** — public HTTPS URL loads; **both themes** (Horror + Sea maps) render and **theme toggle** persists; `/level/{id}` works for a **missing-fallback** level **and** an **available** level; real **Drive image content** streams through the proxy (**key never in browser**, HostR3); audio plays from `static/`; observe/accept **cold-start** behavior (HostQ2/HostR9, R-D5). | project-manager (+ qa-tester) | D6 |
| D8 | **[USER, post-Drive] `POST /api/refresh` once Drive is wired** — one-off `POST https://<app>.onrender.com/api/refresh` to rebuild the discovery map/cache without a redeploy (R2). **Gated on R-D6** (SECURITY): decide auth before public launch — see §10.5. On a spin-down host the cache rebuilds on every cold start anyway. | project-manager + user | D7 |
| D9 | **[AUTO] Document the Render deploy runbook** in `README.md` (Blueprint apply, env-var setup, deploy + `/api/refresh` steps, cold-start note, Drive-sharing precondition) — keep docs in sync (Documentation Policy). | project-manager | D7 |

### Gate (M8)
✅ **Host CHOSEN — Render (D13); Discovery COMPLETE + SIGNED OFF (2026-06-09).** All HostQs resolved: **HostQ2** accept free-tier spin-down; **HostQ3** custom domain via **FreeDNS** (CNAME → `*.onrender.com`, custom domain added in Render); **HostQ5** region **Singapore**; **R-D6** token-gate `POST /api/refresh` (`REFRESH_TOKEN` env var). HostQ4 (keep Drive parent "Anyone with link → Viewer") is a deploy-time user precondition. **Execution-phase diff (M8):** (1) `render.yaml` region `oregon → singapore`; (2) backend token-gate on `/api/refresh` + tests (backend-engineer → SECURITY_ENGINEER → QA_TESTER); (3) `README.md` Render + FreeDNS runbook; (4) the Render deploy itself + live verification. On "go": run the Render deploy + the R-D6 backend change via `orchestrate-build`. CI is green and must stay green (HostR10).

---

## M11 — Image captions (dual-theme) (change request — 2026-06-09)

Status: **SHIPPED (built) — BUILT + SECURITY-APPROVED + QA-VERIFIED (full suite 156 passed / 0 failed); pending commit (PM C9 done, 2026-06-09).** Phase 2 ran end to end: BACKEND_ENGINEER C3 + FRONTEND_ENGINEER C4 + CONTENT C5 → SECURITY_ENGINEER C7 (APPROVED) → QA_TESTER C8 (156 passed) → PM C9 (doc-sync + completeness). **Delivered: 52 real images (levels 1, 2, 8–18) × {sea, horror} = 104 captions** in `app/captions.json`, subject = nickname **"Chudail"** (no real name; a "ANSHIKA" cake-text leak was caught and scrubbed from 13.4). All open questions resolved (REQUIREMENTS §13.5): CapQ1 `(level, filename)`, CapQ2 overlaid `<figcaption class="slide-caption">`, CapQ6 missing-level uncaptioned, CapQ7 `app/captions.json` startup-cached + re-read on `POST /api/refresh` (architect-LOCKED, built as designed); CapQ3 guardrail APPROVED, CapQ4 = "Chudail", **CapQ5 = draft & ship directly (NF-Cap3 review gate WAIVED by the user; user may edit afterward).** Backend (optional, backward-compatible payload), frontend (per-slide slideshow display), content (guardrailed generation) all complete. Missing-level fallback images (levels 0, 3–7) remained out of scope for authored captions (CapR5/CapQ6). **No backend route/payload-breaking change → M8 (Render deploy) status independent and unaffected** (captions ride along as a committed file + an additive payload field).

**Operational notes (NOT code defects — recorded for the operator):**
1. **Restart to load new Python code.** `POST /api/refresh` reloads caption **data** (`app/captions.json`) + Drive discovery, but does **not** reload the Python module — so after editing `captions.json` a refresh suffices, but after a **code** change the running `uvicorn` must be **restarted**.
2. **Transient Drive download throttle.** Drive's download endpoint (`files.get?alt=media`) was returning a 403 rate-limit "Sorry" page after the 52-image caption-generation burst — **transient throttling, recovers on its own**. Drive **metadata** calls still return 200 (key/sharing are fine), so image bytes will display again once the throttle clears. **Unrelated to M11** (no code change; not a defect).

### Milestone
- [x] **M11 — Image captions (dual-theme) — BUILT + SECURITY-APPROVED + QA-VERIFIED (156 passed); pending commit:** A committed caption data file maps each of the **52** real images (levels 1, 2, 8–**18**; level 18 added 2 images 2026-06-09) to `{sea_caption, horror_caption}` (**104** one-liners). Subject referred to as **"Chudail"** (no real name — CapQ4). The active theme selects which caption shows; captions render per-slide in the `level.html` slideshow, theme-styled. Backend payload extension is optional + backward-compatible (no caption → renders as today). Captions are AI-generated by viewing each image under a **hard affectionate/playful-gothic guardrail** (subject is the user's sister, child→18; it's a gift). **Review gate waived (CapQ5) — draft & ship directly**; user may edit after. No regression to slideshow, prefetch, audio, theme isolation, or performance. Security-audited + QA-verified before freeze.

### Open Questions — resolution state (full text in REQUIREMENTS §13.5 / ARCHITECTURE §11.7)
**Architect-LOCKED (no user input needed to proceed):**
- **CapQ1** caption→image key → **`(level, filename)`** (stable across re-upload, read from `PhotoRef.name`, zero new Drive calls; `file_id` rejected) — §11.1.
- **CapQ2** display placement → **overlaid bottom-gradient `<figcaption class="slide-caption">`** per slide, theme-skinned, ≤~70 chars + line-clamp, ≤480px below-stage fallback — §11.4.
- **CapQ6** missing-level fallback images → **uncaptioned** (no stable identity; optional generic placeholder is a later trivial add) — §11.1/§11.2.
- **CapQ7** storage → **`app/captions.json`** (`"<level>" → "<filename>" → {sea, horror}`), loaded once in `lifespan` + cached via `app/captions.py`, re-read on `POST /api/refresh`; missing/malformed → inert (never raises) — §11.2/§11.7.

**USER sign-off blockers (C2) — nothing generated before CapQ3:**
- **CapQ3 (CRITICAL guardrail)** confirm Horror = playful-gothic/affectionate (cute-spooky, never scary/body-focused/sexualizing/unkind) + Sea = funny-sweet; invite tone preferences (nicknames/inside jokes to use or avoid). **Generation MUST NOT start without this sign-off** (R-Cap3).
- **CapQ4 (privacy)** OK to commit captions about a real family member to a **PUBLIC** repo? Avoid her real name + identifying details? (lean: avoid name/details, R-Cap2).
- **CapQ5 (review flow)** confirm: assistant drafts by viewing each image → **50×2 review table** → user edits/approves → only then written to `app/captions.json` and shipped.

### Sequenced backlog (M11 — content + backend + frontend; awaits Discovery sign-off)
Legend: **[USER]** = action requiring the user (guardrail sign-off, privacy decision, caption review/edit); **[CONTENT]** = guardrailed caption generation by viewing images; **[AUTO]** = engineering tasks.
| # | Task | Owner | Depends on |
|---|---|---|---|
| C0 | **✅ (this phase) Capture requirements + open questions** (REQUIREMENTS §13, this milestone). | project-manager | §13 |
| C1 | **✅ DONE (ARCHITECTURE §11).** **Caption data model + integration designed** — CapQ1 key = `(level, filename)` (§11.1); CapQ7 storage = `app/captions.json` shape `"<level>"→"<filename>"→{sea,horror}`, loaded once in `lifespan` + cached via `app/captions.py`, re-read on `POST /api/refresh`, tolerant (missing/malformed → inert) (§11.2); CapR6 payload = additive optional `caption:{sea,horror}` per image in `/api/levels/{id}/photos`, client picks by theme, back-compat proven (§11.3); CapQ2 display = overlaid `<figcaption class="slide-caption">` per slide, theme-skinned, ≤~70 chars + line-clamp, ≤480px below-stage fallback (§11.4); CapQ6 missing-level = uncaptioned (§11.1). Risks R-Cap1–R-Cap6 (§11.8). | solutions-architect | C0 |
| C2 | **[USER] Sign-off gate (BLOCKING — the only thing standing between Discovery and execution).** Obtain explicit user answers to **CapQ3** (content/tone guardrail — Horror playful-gothic/affectionate cute-spooky, Sea funny-sweet; **hard precondition for any generation**; invite tone preferences — nicknames/inside jokes to use/avoid), **CapQ4** (public-repo privacy / avoid real name + identifying details), and confirm **CapQ5** (draft → 50×2 review → ship flow). Architect-LOCKED CapQ1/CapQ2/CapQ6/CapQ7 ride a "go with the recommendations." | project-manager + user | C1 |
| C3 | **✅ DONE.** **Backend payload extension (optional, backward-compatible)** — `app/captions.py` (tolerant loader, startup-cached in `lifespan`, reloads on `POST /api/refresh`, `get_caption(level, filename)`); `app/main.py` `_ref` attaches an optional `caption:{sea,horror}` per image in `/api/levels/{id}/photos`, **omitted when absent** (back-compat, CapR6). Keyed `(level, filename)` from `PhotoRef.name`; no new Drive calls; discovery/cache unregressed (NF-Cap5). | backend-engineer | C1 |
| C4 | **✅ DONE.** **Frontend caption display** — `templates/level.html` renders the active-theme caption per-slide as `<figcaption class="slide-caption">` (set via `textContent` — no HTML injection), theme-scoped in `static/style.css` (`.theme-horror`/`.theme-sea`); `?v=6` cache-bust on both templates; renders nothing when an image has no caption; no clutter on mobile (NF-Cap4); no slideshow/prefetch/audio regression (NF-Cap5). | frontend-engineer | C1, C3 |
| C5 | **✅ DONE.** **Guardrailed caption generation** — assistant viewed each of the **52 real images** (served by the app) and wrote both lines (Sea funny-sweet + Horror playful-gothic) under the NF-Cap1 guardrail; **104 captions** written to `app/captions.json`. Subject = "Chudail" (no real name); a "ANSHIKA" cake-text leak caught & scrubbed from 13.4; 17.0/18.1/18.2 (stylized artwork) captioned to match. | content (assistant) | C2 |
| C6 | **WAIVED (CapQ5).** The mandatory pre-ship user review/edit gate (NF-Cap3) was **waived by the user** — drafted & shipped directly; user may edit `app/captions.json` afterward (a one-line data edit + `POST /api/refresh`). | project-manager + user | C5 |
| C7 | **✅ DONE — APPROVED.** **Security audit** — no secret/Drive exposure, no open-proxy; caption text rendered via `textContent` (no XSS surface); payload back-compat preserved; privacy verified (only "Chudail", **no PII**); all 104 lines pass the affectionate/playful-gothic content guardrail (NF-Cap1/NF-Cap2). | security-engineer | C3, C4, C6 |
| C8 | **✅ DONE — VERIFIED.** **QA** — `tests/test_captions.py` (16 cases): correct-image keying, active theme selects the right line, per-slide update, no-caption renders as before (CapR6), missing-level uncaptioned (CapQ6), back-compat payload, caption escaping, suite stays green. **Full suite 156 passed (was 140).** Live smoke after server restart confirmed captions ARE in the `/api/levels/{id}/photos` payload for **both themes**. | qa-tester | C7 |
| C9 | **✅ DONE.** **Completeness + docs** — full suite green (156 passed); sync README + docs (REQUIREMENTS §13, ARCHITECTURE §11, this section — schema, key strategy, how to add/edit a caption); commit-readiness report produced; the two operational notes recorded (restart-to-load-code; transient Drive download throttle). Nothing committed (user controls commits). | project-manager | C8 |

### Gate (M11)
✅ **BUILT + SECURITY-APPROVED (C7) + QA-VERIFIED (C8, full suite 156 passed / 0 failed) + PM doc-sync done (C9) — pending commit (user's call).** Phase 2 ran end to end: BACKEND_ENGINEER C3 + FRONTEND_ENGINEER C4 + CONTENT C5 → SECURITY_ENGINEER C7 (APPROVED) → QA_TESTER C8 (156 passed, +16 from 140) → PM C9. Delivered **52 images × {sea,horror} = 104 captions** (`app/captions.json`), subject "Chudail" (no PII; "ANSHIKA" cake-text leak scrubbed from 13.4). C6 (NF-Cap3 review gate) **waived by the user per CapQ5** — drafted & shipped directly. Risks: R-Cap1 mitigated (`(level, filename)` key), R-Cap2 closed (no real name / no identifying detail, SECURITY-verified), R-Cap3 closed (all 104 lines pass the affectionate guardrail), R-Cap5 closed (no payload-shape test broke — new caption tests tolerate absence), R-Cap6 acknowledged (operational note: refresh reloads data, code change needs a restart). **No backend route/payload-breaking change → M8 (Render deploy) status independent and unaffected** (captions ride along as a committed file + an additive payload field). **Operational notes (see Status above): (1) restart `uvicorn` to load new Python code (refresh only reloads data); (2) transient Drive download 403 throttle after the generation burst — recovers on its own, not a defect, unrelated to M11.** Nothing committed (user controls commits).

---

## M12 — Build-time image baking (move level images off the per-request Drive proxy) (change request — 2026-06-09)

> **⚠️ SUPERSEDED by M13 (2026-06-09).** The entire M12 Drive build-bake (`scripts/fetch_images.py`, `static/img/levels/` + `manifest.json`, manifest-PRIMARY discovery, the kept `/media` proxy, the periodic download sync) is replaced by **Cloudinary** (see the M13 section below). `app/drive_service.py` + `scripts/fetch_images.py` are deleted; the Drive `alt=media` throttle M12 worked around is moot (no per-request external byte fetch). M12 is retained as history.

Status: **BUILT + SECURITY-APPROVED + QA-VERIFIED (full suite 182 passed / 0 failed; was 156) — pending commit (2026-06-09).** Phase 2 ran end to end via `orchestrate-build`: BACKEND_ENGINEER B3/B4 → PM+BACKEND B5 → SECURITY_ENGINEER B6 (APPROVED) → QA_TESTER B7 (`tests/test_m12_bake_manifest.py`, 26 cases) → PM B8 (doc-sync + smoke). User decided Approach A (bake at BUILD time → serve static; runtime Drive image proxy KEPT only as a guarded fallback) and refined it with a periodic background sync. **As-built:** `scripts/fetch_images.py` (paced bake → `static/img/levels/{id}/{name}` + `missing/`, writes `manifest.json`); `app/drive_service.py` (shared paced `download_images_paced`, `is_safe_name`, `load_manifest`/`load_discovery` manifest-PRIMARY → `DiscoveryCache`, single-flight `run_sync_once`/`background_sync_loop`); `app/main.py` (`_image_sync_interval()`, manifest-PRIMARY lifespan + gated sync task, `_ref` static-if-baked-else-proxy with file_id/name/caption preserved, `/api/refresh` immediate sync); `render.yaml` (`buildCommand` runs the bake + `IMAGE_SYNC_INTERVAL_SECONDS=1800` env); `.gitignore` (`static/img/levels/` ignored). All BakeR1–7 + NF-Bake1–7 satisfied; NF-Bake4 caption key held byte-for-byte (104 captions still attach). **§12.13 background sync shipped:** metadata change-check every `IMAGE_SYNC_INTERVAL_SECONDS` (default **1800**s; **0/unset disables** — hermetic CI/tests), paced delta download only on change, atomic manifest swap; `/api/refresh` forces it → new images appear without a redeploy (NF-Bake6 superseded). **Operational facts (recorded for runbook):** (1) the bake runs at **Render build time** via `buildCommand` using build-env `GD_API_KEY`/`GD_ROOT_FOLDER`; (2) the **background sync runs in prod** (`IMAGE_SYNC_INTERVAL_SECONDS=1800`) — 30-min metadata check, paced delta download only on change, OFF locally/CI (env unset) for hermetic tests; (3) Drive's **download endpoint may still be 403-throttled** from earlier — the paced script + sync back off and retry, so a deploy bake recovers; (4) QA test-seam note: `load_manifest(path=MANIFEST_PATH)` binds the default at def-time (production-correct; a test-seam nicety only) — **no code change required**. **Build precondition (confirm at deploy):** `GD_API_KEY`/`GD_ROOT_FOLDER` set in Render AND Drive stays "Anyone with link → Viewer". **Depends on M11** (captions keyed by `(level, filename)` kept matching — hard gate NF-Bake4 held; 104 captions / suite now 182). **Interacts with M8** (M12 edits only the `render.yaml` `buildCommand` line + the additive `IMAGE_SYNC_INTERVAL_SECONDS` env; M8's only pending `render.yaml` edit is the region line `oregon → singapore` per §12.5 — non-overlapping; M8 region still pending).

### Milestone
- [x] **M12 — Build-time image baking + background sync (build script + static serving + manifest discovery + payload URL change) — BUILT + SECURITY-APPROVED + QA-VERIFIED (182 passed); pending commit:** A build-time fetch script (single source of truth, used by `render.yaml` `buildCommand` AND locally) downloads every available level's images (gently throttled to avoid the 403 burst) into `static/img/levels/{id}/{filename}`, preserving the `{n}.{i}.jpeg` filename so M11 captions still key on `(level, filename)`. Runtime serves them as plain static files; `/api/levels/{id}/photos` `url` switches from the `/media/{file_id}` proxy path to `/static/img/levels/{id}/{filename}` (file_id + name preserved, payload back-compat). The baked dir is git-ignored (personal photos, public repo — NF-Bake1). Discovery reads a build-written `manifest.json` PRIMARY with a Drive-metadata FALLBACK (BakeQ3); the `missing/` set is baked too (BakeQ2); the media proxy is KEPT as a guarded fallback (BakeQ1, recommended — user confirms at B2) with the matching graceful-degrade build-failure policy (BakeQ4). Security-audited (no `GD_API_KEY` in static output/manifest/payload; no photo committed) + QA-verified (static URLs serve, 104 captions still attach, suite ≥156 green) before freeze.

### Open Questions — RESOLVED by the SOLUTIONS_ARCHITECT (B1 DONE); full text in REQUIREMENTS §14.5 / ARCHITECTURE §12
- **BakeQ1** — keep or drop the `/api/levels/{id}/media/{file_id}` proxy? **✅ RESOLVED (user, 2026-06-09): KEEP** as a guarded, non-default fallback; BakeQ4 settles to retry/backoff → WARN + serve-partial. *(§12.5)*
- **BakeQ2 (build-bake)** — bake the `missing/` images too? **✅ RESOLVED: BAKE** into `static/img/levels/missing/`; per-visit re-roll (R3) picks from the local set. *(§12.2/§12.3)*
- **BakeQ2-refinement (background sync) — ✅ RESOLVED (user, 2026-06-09):** beyond the build-bake, add a **periodic background image sync** (architect §12.13): a `lifespan` task every `IMAGE_SYNC_INTERVAL_SECONDS` (default 1800; **0/unset disables — keeps CI/tests hermetic**) does a CHEAP Drive **metadata** list; only when the per-level `file_id` set changed does it download the **delta** with the same paced downloader, atomically swap `manifest.json`, and reload scope → freshness without a redeploy, still no per-request Drive calls and the throttle stays avoided. `POST /api/refresh` forces an immediate sync. New **R-Bake8** (runtime now makes ~30-min metadata calls + holds creds live; interval must be 0 in tests).
- **BakeQ3** — discovery source? **✅ RESOLVED: build-written `manifest.json` PRIMARY** (zero Drive calls at cold start; no creds at runtime), live Drive **metadata FALLBACK**, empty gallery last resort. *(§12.4)*
- **BakeQ4** — build-failure policy? **✅ RESOLVED (settles with BakeQ1): retry/backoff then WARN + serve-partial** if the proxy is KEPT (recommended); **FAIL-the-build** only if the proxy is DROPPED. *(§12.6)*
- **BakeQ5** — filename/URL scheme? **✅ RESOLVED: `static/img/levels/{id}/{PhotoRef.name}` verbatim** — caption key byte-identical; hostile/`..` names rejected, same-folder collisions flagged loudly. *(§12.3)*
- **BakeQ6** — local-dev image source? **✅ RESOLVED: same `scripts/fetch_images.py` + `.env`**; graceful unbaked states (proxy URL when the proxy is kept, else empty gallery — no crash). *(§12.8)*

### Sequenced backlog (M12 — fetch script → static serving + payload URL change → render.yaml buildCommand + .gitignore → keep/drop proxy → captions still match → security → QA → docs)
Legend: **[USER]** = needs the user (real credentials / a "go" on BakeQ1–6); **[AUTO]** = engineering. Owners use the orchestrate-build subagents.
| # | Task | Owner | Depends on |
|---|---|---|---|
| B0 | **✅ (this phase) Capture requirements + open questions** (REQUIREMENTS §14, this milestone). PM framed BakeR/NF-Bake/BakeQ/R-Bake; verified ground truth (`drive_service.py` `resolve_media`/`PhotoRef.name`, `main.py` `_ref` `url`, `app/captions.json` `(level,filename)` key, `render.yaml` `buildCommand`, allow-list `.gitignore` does NOT yet ignore the baked dir). | project-manager | §14 |
| B1 | **✅ DONE — Technical design** (ARCHITECTURE §12). Architect designed `scripts/fetch_images.py` (single source of truth; reuses `drive_service.discover_levels(force=True)` metadata; downloads each image to `static/img/levels/{id}/{PhotoRef.name}`; idempotent w/ `--force`; **paced** — concurrency 2, 0.4s jittered delay, exp-backoff 2→32s, 5 retries on 403/429/5xx — the core fix, NF-Bake2/R-Bake1); the **static layout** + the `_ref` **`url` change** (proxy path → `/static/img/levels/{id}/{name}`, file_id+name preserved — caption key byte-for-byte, BakeR3/NF-Bake4); the **`render.yaml` `buildCommand`** (`pip install -r requirements.txt && python scripts/fetch_images.py`, env vars at build; M12 edits only `buildCommand`, M8 only `region` — non-overlapping); the **`.gitignore`** rule (`static/img/levels/` only; D9 audio allow-list + map/brand assets intact). **Resolved BakeQ2** (bake `missing/`), **BakeQ3** (manifest PRIMARY + metadata FALLBACK), **BakeQ5** (verbatim name), **BakeQ6** (same script locally); **BakeQ1** (keep/drop proxy, recommend KEEP) + **BakeQ4** (settles with BakeQ1) carried to B2. | solutions-architect | B0 |
| B2 | **[USER] Sign-off gate** — confirm **(Q1)** keep vs drop the media proxy (recommend KEEP as a guarded fallback); **(Q2)** redeploy semantics are OK (new/changed images need a redeploy; `/api/refresh` won't pull images — NF-Bake6); **(Q3)** the build precondition holds (`GD_API_KEY`/`GD_ROOT_FOLDER` set in Render AND Drive stays "Anyone with link → Viewer" — D12/HostQ4 — so the BUILD can download). A "go with defaults" suffices. | project-manager + user | B1 |
| B3 | **✅ DONE.** **[AUTO] Build the fetch/bake script** — implemented `scripts/fetch_images.py`: paced Drive download into `static/img/levels/{id}/{PhotoRef.name}` (verbatim) reusing `drive_service` discovery/scope (metadata) + `resolve_media`-style byte fetch with the §12.3 throttle/backoff; **write `manifest.json`** (BakeQ3); **bake the `missing/` set** into `static/img/levels/missing/` (BakeQ2); idempotent (`--force`); runnable locally + from `buildCommand`. **AC (NF-Bake2):** paced so it does not re-trip the 403; failure mode per the BakeQ1 decision (§12.6). **AC (NF-Bake5):** `GD_API_KEY` never written to disk/manifest/logs. **AC (BakeQ5):** reject `/`/`\`/`..` names; flag same-folder collisions loudly. | backend-engineer | B2 |
| B4 | **✅ DONE.** **[AUTO] Static serving + payload URL change + manifest discovery** — `main.py` `_ref` emits `url = /static/img/levels/{id}/{name}` (file_id + name preserved); add a tolerant `manifest.json` loader in `drive_service` feeding the existing `DiscoveryCache` shape, **manifest PRIMARY → Drive-metadata FALLBACK → empty gallery** (BakeQ3, §12.4); `POST /api/refresh` re-reads the manifest; **resolve BakeQ1** per B2 — KEEP the `/media` route + `resolve_media` + byte-LRU as a guarded fallback (R1/R6 ACs in force), or remove them (pure-static). **AC (NF-Bake4):** payload shape unchanged except the `url` value; the `caption` key still attaches via `(level, filename)`. | backend-engineer | B3 |
| B5 | **✅ DONE.** **[AUTO] `render.yaml` `buildCommand` + `.gitignore`** — `buildCommand` runs the bake (`pip install -r requirements.txt && python scripts/fetch_images.py`, env vars present at build) AND the additive `IMAGE_SYNC_INTERVAL_SECONDS=1800` env was added; explicit trailing ignore for `static/img/levels/` added (NF-Bake1, §12.9) **without** regressing the D9 audio allow-list (`!static/audio/**/*.mp3`) or the `static/img/{horror,light,logo}` map/brand assets. **M8 coupling:** edited ONLY the `buildCommand` line + the new env; M8's pending `region` edit untouched — non-overlapping. | project-manager + backend-engineer | B3, B4 |
| B6 | **✅ DONE — APPROVED.** **[AUTO] Security audit (hard gate, veto)** — confirmed `GD_API_KEY` is build/server-side only and absent from the static output, `manifest.json`, payload, and logs (NF-Bake5/R-Bake4); **no photo staged/committed** (`static/img/levels/` ignored; D9 audio allow-list intact, R-Bake2 proven); no path traversal via `{id}`/`{filename}` (StaticFiles sandbox + script `is_safe_name` rejection); the KEPT `/media` proxy's **R1 open-proxy guard is intact** (the `manifest:{lid}` sentinel does NOT bypass scope); sync OFF in tests. | security-engineer | B3, B4, B5 |
| B7 | **✅ DONE — VERIFIED.** **[AUTO] QA** — added `tests/test_m12_bake_manifest.py` (**26 cases**): `/api/levels/{id}/photos` `url` is the static path (shape preserved, tolerant `/static/img/levels/{id}/` prefix, R-Bake7); a baked image serves at `/static/img/levels/{id}/{filename}`; **all 104 M11 captions still attach** via `(level, filename)` (R-Bake3/NF-Bake4); `missing/` re-roll picks from the local baked set (BakeQ2); discovery/`available` semantics unchanged from the manifest (BakeR4); pacing + the BakeQ1-settled failure mode confirmed. **Full suite 182 passed (was 156)** — hermetic (~0.9s, no network; sync OFF). Live smoke: proxy fallback works (nothing baked locally; manifest absent). | qa-tester | B6 |
| B8 | **✅ DONE.** **[AUTO] Completeness + docs + smoke** — full suite green (**182 passed**); local `uvicorn` smoke confirms the proxy fallback path (no local bake / no manifest → images resolve via the guarded proxy, no crash); README image-serving section + project tree + M12 milestone updated (build-time bake → static serving, 30-min background sync, `IMAGE_SYNC_INTERVAL_SECONDS`, `static/img/levels/` git-ignored build output); REQUIREMENTS §14 + ARCHITECTURE §12 (incl. §12.13) + this PLAN section synced to BUILT/verified; the 4 operational facts recorded; completeness report produced. Nothing committed (user controls commits). | project-manager | B7 |

### Gate (M12)
**✅ BUILT + SECURITY-APPROVED (B6) + QA-VERIFIED (B7, full suite 182 passed / 0 failed; was 156) + PM doc-sync done (B8) — pending commit (user's call).** Phase 2 ran end to end: BACKEND_ENGINEER B3/B4 → PM+BACKEND B5 → SECURITY_ENGINEER B6 (APPROVED) → QA_TESTER B7 (26 new cases) → PM B8. Sign-off honored: **BakeQ1 = KEEP** the media proxy as a guarded fallback (R1 open-proxy guard intact); **BakeQ2-refinement = the periodic background image sync shipped** (§12.13; default 1800s, `IMAGE_SYNC_INTERVAL_SECONDS`, 0/unset disables for hermetic tests; `/api/refresh` forces it) → new images appear without a redeploy. All BakeR1–7 + NF-Bake1–7 satisfied; NF-Bake4 caption key held byte-for-byte (104 captions still attach). **Operational facts:** (1) bake at Render build time (`buildCommand`, build-env creds); (2) 30-min background sync in prod (`IMAGE_SYNC_INTERVAL_SECONDS=1800`), OFF locally/CI; (3) Drive download 403-throttle may persist — paced script + sync back off + retry, deploy bake recovers; (4) `load_manifest(path=MANIFEST_PATH)` def-time default is production-correct (test-seam nicety only — no code change). D12 build precondition to confirm at deploy (env vars in Render + Drive "Anyone with link → Viewer"). Depends on M11 (caption `(level, filename)` key kept matching — NF-Bake4 held; 104 captions / suite now 182); interacts with M8 (shared `render.yaml` — M12 edits only `buildCommand` + the additive `IMAGE_SYNC_INTERVAL_SECONDS` env, M8 only `region` which is still pending; non-overlapping). A unit is done only when audited AND verified. CI must stay green (HostR10).

---

## M13 — Google Drive → Cloudinary image-source migration (change request — 2026-06-09)

Status: **BUILT + SECURITY-APPROVED + QA-VERIFIED (full suite 163 passed / 0 failed; was 182) — pending commit (2026-06-09). Supersedes M12 and the Drive parts of M8.** The image source moved off Google Drive entirely onto **Cloudinary**: `app/cloudinary_service.py` makes one server-side Admin API list (metadata only) to discover what exists under `all ages/{N}`, caches it, and a 30-min background task re-lists; the browser then loads each image straight from Cloudinary's **keyless public CDN** (`https://res.cloudinary.com/{cloud}/image/upload/f_auto,q_auto/{public_id}.{format}`). There is **no per-request external fetch and no proxy**, so the Drive `alt=media` download throttle is gone for good. The Drive `/api/levels/{id}/media/{file_id}` proxy, `resolve_media`, the byte LRU, the M12 build-bake (`scripts/fetch_images.py`), and `app/drive_service.py` are **removed** (dead code, deleted in the M13 close-out after SECURITY+QA flagged them as orphaned — suite still 163). `CLOUDINARY_URL` (secret; `api_secret` server-side only — never in payload/logs/browser) replaces `GD_API_KEY`/`GD_ROOT_FOLDER`; `IMAGE_SYNC_INTERVAL_SECONDS=1800` retained; `render.yaml` build is just `pip install -r requirements.txt`, region `singapore`; `.env.example` updated. Captions re-keyed to filename **stems** (`app/captions.json`/`app/captions.py`); all match. QA reworked the suite (deleted `tests/test_cache.py` + `tests/test_m12_bake_manifest.py`, added `tests/test_cloudinary.py`, converted `test_backend.py`/`test_captions.py`, rewrote `conftest.py` to mock the Cloudinary Admin API) → **163 passed**, hermetic. See REQUIREMENTS §15 + ARCHITECTURE §13.

### Milestone
- [x] **M13 — Drive → Cloudinary image source — BUILT + SECURITY-APPROVED + QA-VERIFIED (163 passed); pending commit:** Cloudinary is the sole image source. One Admin API list discovers `all ages/{N}` (level N) + `all ages/missing` (fallback); the browser loads keyless `res.cloudinary.com` CDN URLs directly — no proxy, no per-request external fetch, no credential in the payload/logs/browser. Drive byte-proxy + M12 build-bake + `app/drive_service.py` + `scripts/fetch_images.py` removed (dead code deleted; suite still 163). `CLOUDINARY_URL` replaces `GD_API_KEY`/`GD_ROOT_FOLDER`; captions re-keyed to filename stems; `render.yaml` build = plain `pip install`, region singapore. Payload back-compatible (`url` is now an absolute keyless CDN URL; `file_id` = `public_id`). M8 deploy now needs only `CLOUDINARY_URL`. Security-audited + QA-verified before freeze.

### Gate (M13)
**✅ BUILT + SECURITY-APPROVED + QA-VERIFIED (full suite 163 passed / 0 failed; was 182) + PM close-out (dead-code removal + doc-sync) done — pending commit (user's call).** Image source = Cloudinary keyless CDN; no Drive, no proxy, no build-bake. Dead code (`app/drive_service.py`, `scripts/fetch_images.py`) deleted after confirming nothing imports them — suite re-run still **163 passed**. The `static/img/levels/` `.gitignore` rule was removed (the bake no longer exists); no leftover working-tree dir. Docs synced: README (image source, data-flow, tech-stack, endpoints, config, structure, M13 milestone), REQUIREMENTS §15 + supersession pointers on F8/F10/D2/D6/D9/D10/D12/§6/§7/§12/§14, ARCHITECTURE §2/§3/§4/§5 + new §13, this PLAN section. **Deploy note:** M8 now needs only `CLOUDINARY_URL` set in the Render dashboard (build is plain `pip install`; the Admin `api_secret` is server-side; delivery is keyless CDN; region singapore) — this fixes the Drive throttle for good. No Google Drive vars, no folder-sharing precondition. Supersedes M12 and the Drive parts of M8. A unit is done only when audited AND verified. CI must stay green.

---

## M14 — Dynamic, atmospheric individual level/island pages (change request — 2026-06-09)

Status: **✅ SHIPPED (2026-06-09) — L3–L8 COMPLETE: frontend implemented, SECURITY-APPROVED, QA-VERIFIED (full suite 185 passed, hermetic), local `uvicorn` smoke test PASSED (both themes × both states, HTTP 200, no 500s); pending commit (user controls commits).** Resolved defaults shipped as recommended: pure CSS + inline SVG (raster DEFERRED — Q1, no `static/img` env asset added; L5 SKIPPED), evocative-but-recognizable room/shore (Q2), pure CSS/SVG frame on `.level-stage` via `.frame-corner`/`.frame-corner--*` (Q3, A5 = CSS), BOTH states (Q4), subtle/sparse reduced-motion-gated decor (Q5), uniform per theme (Q6, A4 = uniform), **NO new SSR var — `app/main.py` untouched** (Q7). Frontend-only redesign of the per-level page (`templates/level.html` + theme-scoped `static/style.css`) so `/level/{id}` has the immersive, full-bleed, decorated atmosphere the M9/M10 landing **maps** already have — replacing the old "one small framed image floating in a vast flat dark/empty backdrop." Both themes, both states (available slideshow **and** sealed/missing fallback). New classes shipped: `level-env-*` (`-wall`/`-floor` Horror, `-sky`/`-sea`/`-sand` Sea), `level-ambient` (neutral level-page decor wrapper), `sea-scene` + `sea-silhouette--*` (inline-SVG Sea level-page scene, twin of `.horror-scene`, with `<radialGradient id="sea-sun-lvl">`), `frame-corner--*`, `level-note`; `style.css?v=6 → ?v=7` on BOTH `templates/level.html` and `templates/index.html`. Requirements: REQUIREMENTS §16 (M14R1–M14R9, NF-M14-1…8, Q-M14-1…7, R-M14-1…6). **No backend/route/payload/Cloudinary/discovery/audio-engine change — `app/main.py` untouched** (the permissible additive SSR field was NOT taken; uniform-per-theme needs no new var — §14.6). The M13 Cloudinary reality is ground truth (no Drive, no proxy). **The existing `level.html` test contracts were a HARD GATE (NF-M14-7) and all SURVIVE:** `var levelId`, `var ssrAvailable`, `fetch("/api/levels/" + levelId + "/photos"`, `id="slideshow"`, `id="theme-toggle"`, the `salvaged fallback content` note text, the `.atmosphere` aria-hidden / `horror-scene` / `focusable="false"` / no-interactive / no-`tabindex` invariants (Horror AND the new Sea `.sea-scene`), and the M11 `.slide-caption` display — confirmed by the 185-green suite (incl. the 21 new `tests/test_m14_level_atmosphere.py` regression tests). All M9/M10/M11/M13 features + locked decisions preserved. **M14 change set:** `templates/level.html`, `static/style.css`, `templates/index.html` (`?v=7` link bump), the three docs (this PLAN, REQUIREMENTS §16, ARCHITECTURE §14, README), `design/` artifacts, and three test files (`tests/test_m14_level_atmosphere.py` NEW; `tests/test_captions.py` relaxed to a `?v=\d+` regex (A2); `tests/test_horror_atmosphere.py` M9 blanket `<radialGradient>` guard relaxed so the Sea LEVEL page may carry `.sea-scene`/`sea-sun-lvl` — it still asserts no Horror decoration leaks into Sea, and the landing `/` case is unchanged).

### Milestone
- [x] **M14 — Dynamic, atmospheric level/island pages (frontend-only) — SHIPPED (2026-06-09; 185 passed):** Full-bleed theme-appropriate environment behind the level stage (haunted room for Horror; sunlit island/shore for Sea), the photo slideshow presented as a themed framed focal centerpiece (gilded/ornate vs driftwood/natural), ambient motion decor reusing the landing's vocabulary (`.horror-ghost`/`-twinkle`/`-star`/`.horror-scene`; `.sea-balloon`/`-bird`/`-cloud` — the latter brought to the Sea level page where it currently renders nothing), an **atmospheric sealed/fallback state** (framed "Chudail-not-found" + diegetic restyled `salvaged fallback content` note, not a void), restyled heading/note/footer + preserved logo-home / theme-toggle / audio controls, responsive mobile→desktop, `prefers-reduced-motion` honored. Delivered via the existing `.theme-horror`/`.theme-sea` tokens + `--page-bg-image`/`--vignette` + `.atmosphere`(z-0)/`.layer`(z-1) system; slideshow + M11 captions intact. Security-audited (scope/asset/aria) + QA-regression-verified (all `level.html` contracts green) before freeze.

### Open Questions — RESOLVED by SOLUTIONS_ARCHITECT (ARCHITECTURE §14); user confirm/deny on A1/A4/A5 (full text in REQUIREMENTS §16.5)
All Q-M14-1…7 are now technically RESOLVED; the architect firmed the technical defaults in ARCHITECTURE §14 and the PM reconciled REQUIREMENTS §16.5. What the user must decide collapses to three confirm/deny items (A1/A4/A5):
- **Q-M14-1 — RESOLVED → pure CSS + inline SVG; illustrated raster DEFERRED to a `--page-bg-image` hook (§14.2).** *(firmer than the PM "optional one webp" lean)* — **A1: confirm at sign-off** (flip to L5 + ≤300 KB/theme WebP if the user wants literal painted scenery now).
- **Q-M14-2 — RESOLVED → evocative-but-recognizable** (Horror moonlit room bands; Sea sunlit-shore field).
- **Q-M14-3 — RESOLVED → pure CSS/SVG frame on `.level-stage`** (gilded Horror, driftwood/rope Sea) — **A5: confirm** (a photographed frame texture would add a small raster, same budget as A1).
- **Q-M14-4 — RESOLVED → BOTH** available-slideshow AND sealed/missing fallback.
- **Q-M14-5 — RESOLVED → subtle/sparse**, reuse landing drift counts/speeds, reduced-motion-gated.
- **Q-M14-6 — RESOLVED → uniform per theme** (one room, one shore for all level ids) — **A4: confirm** (per-level variation would reopen Q-M14-7 + expand effort).
- **Q-M14-7 — RESOLVED → NO new SSR var; `app/main.py` untouched** (uniform scene → existing `theme`/`level_id`/`available`/`audio_track_ids` suffice; §14.6). Only reopens if A4 flips.
- **A2 (engineering-internal, not a user Q):** bump `style.css?v=6 → ?v=7` on BOTH templates (contract-safe; guard accepts any `?v=\d+`) — SECURITY scope-audit (L6) expects the two `<link>` edits.
- **A3 (engineering-internal, verified):** ambient leaf rules are authored `.theme-* .X--N` (theme-scoped, container-agnostic — confirmed in `style.css`), so the level page ADDS `.theme-* .level-ambient .X--N` positions and never edits a landing rule; QA (L7) confirms the landings stay visually unchanged.

### Sequenced backlog (M14 — Discovery → architect design → UI design → frontend → security → QA → PM verify)
Legend: **[USER]** = user sign-off on Q-M14-1…7; **[AUTO]** = engineering via the orchestrate-build subagents. The TD design step (ui-designer, per the standing rule) precedes frontend implementation.
| # | Task | Owner | Depends on |
|---|---|---|---|
| L0 | **✅ (this phase) Capture requirements + open questions** (REQUIREMENTS §16; this milestone). PM read `level.html`/`index.html`/`style.css`/`main.py` + docs; framed M14R/NF-M14/Q-M14/R-M14; verified ground truth (level page already has the `.atmosphere`/`horror-scene` hooks; Sea level page has NO atmosphere; landing ambient classes not reused on the level page; the hard test contracts in §16.2). | project-manager | §16 |
| L1 | **✅ DONE — Technical design** (ARCHITECTURE §14, authored by the architect). Resolved Q-M14-1 → pure CSS + inline SVG, raster DEFERRED to `--page-bg-image` hook (§14.2); Q-M14-2 → evocative-but-recognizable; Q-M14-3 → pure CSS/SVG frame on `.level-stage` (§14.4); Q-M14-6 → uniform per theme; Q-M14-7 → NO new SSR var (§14.6). Authored the level-page `.atmosphere` layering model (§14.3) with a NEW Sea `.sea-scene` (twin of `.horror-scene` — aria-hidden/focusable=false/inert), the NEW neutral `.level-ambient` decor wrapper + `level-env-*` band family, the generalized R-H5 scrim for both themes, reduced-motion + responsive plans (§14.5), the zero-backend-change proof (§14.6), and the contract-preservation checklist (§14.7). Raised flags A1–A5. | solutions-architect | L0 |
| L2 | **[USER] Sign-off gate** — confirm the architect's resolved defaults; only **A1** (raster vs pure-CSS), **A4** (uniform vs per-level), **A5** (frame raster vs CSS) genuinely need a user call (A2/A3 are engineering-internal). A "go with the recommendations" suffices to unblock execution. | project-manager + user | L1 |
| TD | **Design phase** (`design-webpage` skill → `ui-designer`, standing step before frontend) — produce the level-page design: environment + framed-centerpiece + ambient decor mockups for BOTH themes (Horror room, Sea shore), available + fallback states, with the theme toggle working; tokens/frame spec feeding `design/`. User look/feel sign-off. | ui-designer | L2 |
| L3 | **✅ DONE — Frontend implementation** — `templates/level.html`: add the full-bleed environment + framed-photo centerpiece + theme-symmetric ambient decor inside `.atmosphere` (Horror reuse/extend `.horror-scene` + `.horror-ghost`/`-twinkle`/`-star`; **Sea: add the NEW inline-SVG `.sea-scene`** in a `{% if theme == 'sea' %}` branch — the structural twin of `.horror-scene`, aria-hidden/focusable=false/inert/no-tabindex per §14.3), the reused ambient spans in the NEW neutral **`.level-ambient`** wrapper (NOT the landing's `.horror-ambient`/`.sea-ambient`), the NEW `level-env-*` environment bands, the atmospheric sealed/fallback presentation, restyled heading/note/footer; bump `style.css?v=6 → ?v=7` on this template AND `index.html` (A2). PRESERVE every contract in §16.2 / ARCHITECTURE §14.7 (`var levelId`, `var ssrAvailable`, the literal photos `fetch(...)`, `id="slideshow"`, `id="theme-toggle"`, the `salvaged fallback content` note text, the slideshow JS, the M11 `<figcaption class="slide-caption">`). **AC (NF-M14-7):** no asserted token renamed/removed; `app/main.py` NOT touched. | frontend-engineer | TD |
| L4 | **✅ DONE — Frontend CSS** — theme-scoped `static/style.css` rules: `.theme-horror` / `.theme-sea` level-page environment (`--page-bg-image`/`--vignette` + the `.level-env-*` scene bands + the `.sea-scene` SVG styling), the framed `.level-stage`/slideshow mount + backing scrim (R-M14-1 legibility guard generalized to BOTH themes — add the symmetric `.theme-sea .level-stage` opaque backing), the level-page ambient decor styling via **ADDED `.theme-* .level-ambient .X--N` per-instance positions** (A3 — reuse the landing leaf classes/keyframes; NEVER edit a landing `.X--N` rule; new Sea level-page instance must not alter the Sea landing — R-M14-3), responsive mobile breakpoints (frame/decor scale-down via `clamp()`), and an extension of the global `@media (prefers-reduced-motion: reduce)` block to freeze all NEW motion (reused spans inherit the existing freeze). **AC (NF-M14-5):** every rule theme-scoped; no `:root` neutral edits; no `.horror-map`/`.sea-map`/`.horror-ambient`/`.sea-ambient` (landing) changes. **AC (NF-M14-3):** decor CSS/SVG/DOM-driven; no raster under the default. | frontend-engineer | TD |
| L5 | **⏭️ SKIPPED — CONDITIONAL on A1; not triggered (pure-CSS/SVG default kept, A1 not flipped).** No `static/img` environment/frame asset was added; the `--page-bg-image` hook remains available but unused. (Mirrors M9 H5.) Add + optimize the per-theme environment (and/or A5 frame) asset(s) under `static/img/{horror,light}/...` wired ONLY through the `--page-bg-image` hook, document source/license + optimization origin, verify the **≤300 KB/theme WebP** budget (R-M14-4). **SKIPPED under the recommended pure-CSS/SVG default** (mirrors M9 H5). | frontend-engineer | L1, L4 |
| L6 | **✅ DONE — Security audit (scope/asset/aria veto) — APPROVED.** Diff surface confirmed presentational only (`templates/level.html` + `static/style.css` + the `templates/index.html` `?v=7` link bump); `app/main.py` untouched (no new SSR var); no secret/Cloudinary/payload/XSS surface; the new `.sea-scene` + `.level-ambient` decor is inert (`aria-hidden`, `focusable="false"`, no `tabindex`, no interactive). No `static/img` asset added (A1 not flipped). No out-of-scope edit. — confirm the diff surface is exactly {`templates/level.html` level-page redesign + the two `style.css?v=7` `<link>` bumps (A2) + theme-scoped `static/style.css` + any budgeted `static/img` asset ONLY if A1 flipped} and nothing else; **`app/main.py` untouched (NO new SSR var — Q-M14-7);** no secret/Cloudinary/payload exposure; no new interactive/`tabindex` in any decorative layer (incl. the NEW `.sea-scene` + `.level-ambient`); decorative layers `aria-hidden`; any committed asset within budget. Veto on any out-of-scope edit (landing maps, backend, audio, payload). | security-engineer | L3, L4, L5 |
| L7 | **✅ DONE — QA regression + new tests — VERIFIED (185 passed, hermetic).** Added `tests/test_m14_level_atmosphere.py` (21 M14 regression tests across both themes + both states); relaxed `tests/test_captions.py` to a `?v=\d+` regex (A2); relaxed `tests/test_horror_atmosphere.py`'s M9 blanket `<radialGradient>` guard so the Sea LEVEL page may carry `.sea-scene`/`sea-sun-lvl` (still asserts no Horror decoration leaks into Sea; the landing `/` case unchanged). Every preserved `level.html` contract green; M9 Horror + M10 Sea landings unchanged. — assert every preserved `level.html` contract (§16.2) is intact for BOTH themes + BOTH states (available/fallback): `theme-*` classes, `var levelId`, `var ssrAvailable`, the photos `fetch(...)`, `id="slideshow"`, `id="theme-toggle"`, the `salvaged fallback content` note, the `.atmosphere` aria-hidden / focusable=false / no-interactive / no-`tabindex` (Horror AND the new Sea scene), M11 captions still render; the M9 Horror landing + M10 Sea landing remain byte-/behaviour-unchanged (R-M14-3); `prefers-reduced-motion` freezes all new motion (R-M14-5); WCAG AA contrast of heading/caption/note/footer/toggle/controls over the new scene (R-M14-1); responsive mobile pass — photo + caption + controls stay clear (R-M14-6); committed asset within budget (R-M14-4); full suite stays green (CI, HostR10). | qa-tester | L6 |
| L8 | **✅ DONE — Completeness + docs + smoke.** Full suite **185 passed** (hermetic, ~2s). Local `uvicorn` smoke on `127.0.0.1:8019` PASSED — `/`, `/level/1`, `/level/3` for BOTH themes (default→horror; `theme=sea` cookie) all HTTP 200, no 500s: Horror level page carries `horror-scene` + `level-env-wall`/`-floor` and NOT `sea-scene`; Sea level page carries `sea-scene` + `level-env-sky`/`-sea`/`-sand` and NOT a `horror-scene` class element (the only `horror-scene` match on the Sea page is inside an HTML comment naming the twin); `frame-corner` renders both themes; the sealed page (`/level/3`, empty cache locally) carries both `salvaged fallback content` and `level-note`; `style.css?v=7` linked on `/` and `/level/{id}`. README + ARCHITECTURE §14 + REQUIREMENTS §16 + this PLAN synced (new tokens/classes documented; M9 Sea-scene claim reconciled — Documentation Policy). Nothing committed (user controls commits). | project-manager | L7 |

### Gate (M14)
**✅ COMPLETE (2026-06-09) — all of L0–L8 done; pending commit (user controls commits).** Discovery (L0/L1) + USER sign-off (L2, recommended defaults accepted: A1 = pure-CSS, A4 = uniform, A5 = CSS frame) → ui-designer (TD) → frontend-engineer (L3/L4; **L5 SKIPPED — A1 not flipped**) → security-engineer (L6, APPROVED) → qa-tester (L7, 185 passed) → PM (L8 smoke PASSED + doc-sync). Frontend-only; no backend/route/payload/Cloudinary/audio change — **`app/main.py` is NOT touched** (NO new SSR var; uniform-per-theme needs none — §14.6). The `level.html` test contracts (HARD GATE, NF-M14-7) all survive; CI is green. A unit was done only when audited AND verified — both held.

---

## M16 — Chest-chooser gateway landing (change request — 2026-06-13)

Status: **BUILT + SECURITY-APPROVED + QA-VERIFIED (full suite 220 passed) — pending commit (2026-06-13). Phase 2 G3–G8 all DONE.** NEW milestone: replaced `/` (formerly the per-theme illustrated MAP) with a **neutral, theme-agnostic "gateway" landing** — a horror×fantasy mix carrying a transparent lowered cursive "choose a chest" caption (`Cinzel Decorative`) over a **painted raster backdrop** (`static/img/gateway/gateway-bg.jpg`, ~369 KB) and **three chests** (moon / half-sun-half-moon / sun; middle largest). Opening a chest plays a ghost (→ Horror) or cake (→ Sea) zoom-to-fullscreen reveal, **sets the `theme` cookie via `Archive19Theme.choose()` BEFORE navigating**, and lands on that theme's existing map — now relocated to a **SUBPAGE** at `/map/horror`+`/map/sea` (theme FORCED by route, D15). Optional gateway ambient (`static/audio/global/gateway_ambient.mp3`) is gesture-gated + fail-silent. **As-built revision:** D24's pure-CSS+SVG reveal art shipped instead as a budgeted raster backdrop like the M9/M10 landing maps (REQUIREMENTS §17 as-built note / ARCHITECTURE §17 D24 revision). All other M16 decisions D14–D23 shipped as designed; `style.css?v=8`→`?v=9` on all three templates. **The SOLUTIONS_ARCHITECT authored the technical design (ARCHITECTURE §17, decisions D14–D24); PM reconciled it against REQUIREMENTS §17 — no unmet requirement, no contradiction; the user signed off at G2 (2026-06-13).** **G2 sign-off answers:** (1) Q-M16-1 chest-2 = **RANDOM 50/50** (confirms D21); (2) Q-M16-4 route names = **`/map/horror` + `/map/sea`** (OVERRIDES the architect's recommended bare `/horror`+`/sea`; `/` always the gateway, no redirect — confirms D17); (3) Q-M16-7 = **show the gateway every visit** (never auto-skipped; a stored cookie only pre-selects theme for maps/levels). The 9 shaping Q's (Q-M16-2/3/5/6/8/9/10/11/12) all ride the architect defaults. Route shape (D14, as signed off): `/` → NEW theme-neutral `gateway.html` (no `.theme-*` class, ignores the cookie for its own render — D16, no flash); the existing map (`index.html`) relocates to **`/map/horror`** and **`/map/sea`**, each **FORCING** its theme literal (D15); `/level/{id}` + `/api/*` UNCHANGED; **no redirects** — `/` always 200s as the gateway (D17). SSR kept (D18); both subpages re-render the EXISTING `index.html` byte-for-byte with a forced theme. Cookie-before-navigate (D19/D20): an additive `Archive19Theme.choose(theme)` writer (writeCookie + writeSession, NO reload) is added to `theme.js` — `reconcile()` core is NOT edited; a NEW `static/gateway.js` does `choose(theme)` → reveal → `location.assign("/map/horror"|"/map/sea")`. Chest-2 (D21): one client `Math.random()<0.5` per click drives BOTH the reveal art AND the destination. Reveal (D24): pure CSS+SVG scale/opacity zoom-to-fullscreen; `animationend`→navigate with an `hasNavigated`-guarded fallback timer; reduced-motion skips the reveal (D22). **This is the first route/SSR change since M13 — `app/main.py` IS touched** (old `index()` → `gateway()` + two thin map handlers; `read_theme`/`_levels_payload`/`_local_audio_ids`/lifespan untouched — D14/§17.2). **Biggest regression surface = the test re-point (NF-M16-8):** the architect produced a CONCRETE migration map (§17.7) — re-point the map assertions in `tests/test_frontend.py`/`test_horror_atmosphere.py`/`test_sea_landing.py` from `/` to `/map/horror`/`/map/sea`; ALL `/level/*`, `test_m14_level_atmosphere.py`, `test_captions`/`test_cloudinary`/`test_backend` UNCHANGED; ADD a NEW `tests/test_gateway.py`. Requirements: REQUIREMENTS §17 (M16R1–M16R14, NF-M16-1…9, Q-M16-1…12, R-M16-1…6); design: ARCHITECTURE §17 (D14–D24, test-migration map §17.7). Builds on and must not regress M9/M10 maps, M14 level atmosphere, M15 level-18 lyrics, M11 captions, M13 Cloudinary. **Next step: the TD design phase (`design-webpage` skill → `ui-designer`), then Phase 2 build G3→G8 via `orchestrate-build`.** New architect risks R-M16-7…10 in the register below.

### Milestone
- [x] **M16 — Chest-chooser gateway landing (BUILT + SECURITY-APPROVED + QA-VERIFIED, 220 passed — pending commit 2026-06-13):** `GET /` renders a NEW neutral gateway (cursive "choose a chest" + three chests, middle largest, fits phone→PC); chest 1 (moon) → ghost zoom → Horror map; chest 3 (sun) → cake zoom → Sea map; chest 2 (half-sun/half-moon, largest) → ghost OR cake (rule = Q-M16-1) → matching theme map. Each chest = symbol only, no text, keyboard-activable with `aria-label`. Selection sets the `theme` cookie + sessionStorage (D1/D3) BEFORE navigating so the destination map + subsequent `/level/{id}` pages SSR-render flash-free. The two existing per-theme map landings move to SUBPAGE routes (Q-M16-4), still standalone-deep-linkable, toggle intact (Q-M16-9). Pure CSS + inline SVG by default (Q-M16-3); `prefers-reduced-motion` skips/instant-resolves the reveal. No new secret (NF-M16-7); `/level/{id}`, audio engine, Cloudinary, payload all unchanged. Existing map tests re-pointed to subpages + new gateway tests added (NF-M16-8); full suite must end green. Doc sync + `style.css?v=` bump. Security-audited + QA-verified before freeze.

### Open Questions — RESOLVED at G2 sign-off (2026-06-13) — (full text in REQUIREMENTS §17.5; architect decisions in ARCHITECTURE §17.8)
All 12 Q's are resolved. The 3 BLOCKING items carry the user's explicit answers; the 9 shaping Q's go with the architect defaults D14–D24. The one OVERRIDE of an architect recommendation is Q-M16-4 (route namespace `/map/{theme}`).

**BLOCKING — ANSWERED at G2 (2026-06-13):**
- **Q-M16-1 — chest-2 "ghost OR cake" rule → RANDOM 50/50 (user, confirms D21).** Client `Math.random()<0.5` per open; the SAME outcome drives BOTH the reveal art AND the destination (ghost→Horror, cake→Sea). No split-halves variant.
- **Q-M16-4 — subpage route names + is `/` ALWAYS the gateway → `/map/horror` + `/map/sea` (user, OVERRIDES the architect's recommended bare `/horror`+`/sea`).** The two relocated maps live under the `/map/` namespace; `/` is ALWAYS the gateway, **no redirect** (confirms D17). The backend route shape AND the test re-point (§17.7) are updated to `/map/{theme}`.
- **Q-M16-7 — show the gateway every visit, or skip-on-stored-cookie → SHOW EVERY VISIT (user, confirms default).** The gateway is never auto-skipped; a stored `theme` cookie only pre-selects theme for the maps/levels. No `/` redirect (D17).

**Shaping — RESOLVED → architect defaults D14–D24 (user went with the recommended defaults at G2):**
- **Q-M16-2** reveal art fidelity/motion → **D24: stylized inline-SVG ghost/cake, scale+fade (zoom) to fullscreen.**
- **Q-M16-3** asset approach (CSS+SVG vs raster/Lottie) → **D24: pure CSS + inline SVG** (no new raster, no new JS dependency, no new secret — NF-M16-7).
- **Q-M16-5** cursive font for the gateway heading → lean a **single neutral cursive** (reuse `Tangerine` or add ONE neutral face via the M15 Google-Fonts pattern; non-blocking, system-cursive fallback).
- **Q-M16-6** gateway audio (silent vs ambient; transition sting) → lean **silent gateway** + optional gesture-gated open sting; destination ambient starts on the map as today.
- **Q-M16-8** subpage deep-link behavior → **D15/D23: the subpage FORCES its theme AND writes the matching cookie on arrival; no `/` redirect (D17).**
- **Q-M16-9** keep the in-page toggle on subpages → **lean KEEP** (don't regress F2 "toggle on every page").
- **Q-M16-10** home/logo target → **lean logo → `/` (gateway)**; note this is a UX behavior change (was "home = your map") — see R-M16-8.
- **Q-M16-11** chest visual identity → lean **uniform chests, differentiated by symbol only** (per "nothing written on it, only a symbol").
- **Q-M16-12** gateway ignores a pre-existing cookie for its own render → **D16: yes** (theme-neutral `.gateway` render, no flash from a stale cookie).

### Sequenced backlog (M16 — Phase 1 Discovery now; Phase 2 executes on sign-off)
Legend: **[USER]** = needs a user decision (the BLOCKING Q's / a "go with defaults"); **[AUTO]** = engineering via the orchestrate-build subagents. The TD design step (ui-designer, standing rule) precedes frontend implementation.
| # | Task | Owner | Depends on |
|---|---|---|---|
| G0 | **✅ (this phase) Capture requirements + open questions** (REQUIREMENTS §17, this milestone). PM read CLAUDE.md/README/REQUIREMENTS/PLAN/ARCHITECTURE + `app/main.py`/`static/theme.js`/`static/style.css`/`templates/index.html`/`templates/level.html`; framed M16R/NF-M16/Q-M16/R-M16; verified ground truth (`/` is the themed MAP today; D1/D3 cookie owned by `static/theme.js`, SSR only reads it via `read_theme`; map tests assert behavior at `/` → NF-M16-8 re-point needed; M14/M9 pure-CSS+SVG ethos; M15 cursive fonts level-18-only). | project-manager | §17 |
| G1 | **✅ DONE — Technical design (ARCHITECTURE §17, decisions D14–D24).** Architect designed: the **route topology + SSR** (§17.1 — D14 `/`=neutral gateway, maps→`/map/horror`+`/map/sea` (route namespace set at G2); D15 subpages FORCE their theme; D16 gateway ignores the cookie for its own render; D17 no redirects, `/` always 200s; FastAPI signatures); the **template split** (§17.2 — D18 NEW `templates/gateway.html` + serve the EXISTING `index.html` byte-for-byte at BOTH subpages with a forced theme, NO `index.html` refactor; the minimal `app/main.py` diff shape); the **cookie-before-navigate flow** (§17.3 — D19 additive `Archive19Theme.choose(theme)` no-reload writer in `theme.js`, `reconcile()` core untouched; D20 NEW `static/gateway.js`; D23 subpage reconcile trusts the SSR-forced theme over a stale cookie); the **chest-2 rule** (§17.4 — D21 client `Math.random()<0.5` per click drives reveal+destination together; cheap split-halves variant flagged); the **reveal architecture** (§17.5 — D24 pure CSS+SVG scale+fade to fullscreen, `animationend`→navigate with an `hasNavigated`-guarded fallback timer; D22 reduced-motion skips the reveal); the **data-flow diagram** (§17.6); and the **concrete test-migration map** for NF-M16-8 (§17.7 — exact files/tests to re-point + the NEW `tests/test_gateway.py` cases). Raised confirm/deny flags for the 3 BLOCKING Q's + new risks R-M16-7…10 (§17.8). | solutions-architect | G0 |
| G2 | **✅ DONE — [USER] Sign-off gate (signed off 2026-06-13).** User answered the **3 BLOCKING** questions: **Q-M16-1** chest-2 rule = **RANDOM 50/50** (confirms D21); **Q-M16-4** route names = **`/map/horror` + `/map/sea`** (OVERRIDES the recommended bare `/horror`+`/sea`; `/` always the gateway, no redirect — confirms D17); **Q-M16-7** = **show every visit** (gateway never auto-skipped; cookie only pre-selects theme for maps/levels). The 9 shaping Q's (Q-M16-2/3/5/6/8/9/10/11/12) all ride the architect defaults D14–D24. **R-M16-8** (brand-logo `href="/"` now lands on the gateway, not "your map") accepted as the intended UX change. **Discovery (Phase 1) is now COMPLETE — TD + G3–G8 are UNBLOCKED.** | project-manager + user | G1 |
| TD | **[NEXT STEP · UNBLOCKED by G2] Design phase** (`design-webpage` skill → `ui-designer`, standing step before frontend) — produce the gateway design: the horror×fantasy-mix neutral backdrop, the cursive "choose a chest" heading, the three chests (moon / half-sun-half-moon / sun, middle largest) responsive phone→PC, and the ghost + cake zoom-to-fullscreen reveal storyboards, plus the relocated map subpages' unchanged look. User look/feel sign-off. | ui-designer | G2 |
| G3 | **✅ DONE — Backend routes/SSR** — implement the architect's route shape (§17.1/§17.2, D14–D18) in `app/main.py`: REPLACE `index()` with a theme-neutral `gateway()` at `/` (NO `read_theme` — D16) rendering `gateway.html` with an empty context; ADD `map_horror()`/`map_sea()` that render the EXISTING `index.html` with a FORCED `theme` literal + `_levels_payload()` (D15/D18); **NO redirects** (D17); `read_theme`/`_levels_payload`/`_local_audio_ids`/lifespan/`/level/*`/`/api/*` ALL untouched. **AC (NF-M16-4/5):** the subpages emit byte-identical map HTML to today's `/` (only the URL changed); the gateway renders theme-neutrally (no `.theme-*` on `<html>`/`<body>`) regardless of any existing cookie (Q-M16-12/D16). | backend-engineer | TD |
| G4 | **✅ DONE — Gateway template + (no map refactor)** — *(as-built: ghost/cake reveal shipped over a painted RASTER backdrop `static/img/gateway/gateway-bg.jpg`, not pure inline SVG — D24 revision; `index.html` gained `data-force-theme="{{ theme }}"`)* — add NEW `templates/gateway.html` (D18): neutral `.gateway`-scoped markup with NO `.theme-*` class, the cursive `<h1>choose a chest</h1>`, three chests as accessible `<button>`/`<a>` with non-empty `aria-label`s + moon/half-sun-half-moon/sun symbol SVGs (middle largest), the ghost/cake reveal SVG scaffolding (`aria-hidden`/`focusable="false"`/inert), loads `theme.js` then `static/gateway.js`, `style.css?v=9` bump; `index.html` is REUSED at the subpages with NO structural change (D18 — only the route serving it moved). **AC (NF-M16-2/6):** chests keyboard-activable (Enter/Space); gateway styles scoped, no `.theme-*` bleed, no `:root` edit (R-M16-10). | frontend-engineer | TD, G3 |
| G5 | **✅ DONE — Gateway CSS + reveal/cookie JS** — *(as-built: theme.js also gained a `data-theme-neutral` gateway guard + `data-force-theme` map-toggle wiring (the subpage toggle switches MAPS); gateway ambient `static/audio/global/gateway_ambient.mp3` present, gesture-gated + fail-silent)* — (a) extend `static/theme.js` with the additive no-reload **`Archive19Theme.choose(theme)`** writer (writeCookie+writeSession, NO reload — D19); **do NOT edit `reconcile()` core** (R-M16-7); (b) NEW `static/gateway.js` (D20): chest wiring + the per-click chest-2 `Math.random()<0.5` resolution (D21) driving BOTH reveal art + destination, `choose(theme)` → reveal → `location.assign("/map/horror"|"/map/sea")`, with the `hasNavigated`-guarded `animationend`+fallback-timer handoff (D24/R-M16-9) and the reduced-motion instant-resolve skip (D22); (c) scoped `.gateway` CSS (responsive chests via `clamp()`/flex/grid, middle largest, fits any device — NF-M16-1; the GPU-cheap transform/opacity zoom-to-fullscreen reveal; cursive font load per Q-M16-5; gateway audio policy per Q-M16-6); (d) the subpage arrival reconcile so a forced-theme render wins over a stale cookie (D23) — smallest form, no `reconcile()` core edit. **AC (NF-M16-2):** `prefers-reduced-motion` skips the reveal, no flashing. **AC (NF-M16-3):** pure CSS+SVG default; any opted-in raster/Lottie budgeted. **AC (NF-M16-4):** the cookie/session write is byte-compatible with `theme.js` (`theme=horror\|sea; path=/; max-age=…; SameSite=Lax`). | frontend-engineer | G4 |
| G6 | **✅ DONE — SECURITY APPROVED — Security audit (scope/secret/aria veto)** — confirm the diff is exactly {`app/main.py` route change (D14/§17.2), NEW `templates/gateway.html`, the additive `theme.js` `choose()` export + the subpage reconcile tweak (D19/D23 — NO `reconcile()` core rewrite), NEW `static/gateway.js`, scoped `.gateway` CSS in `static/style.css`, `style.css?v=9` bumps, doc sync} and nothing else; **no new secret/credential/external dependency** (NF-M16-7), Cloudinary `api_secret` still server-side; no proxy opened; decorative gateway/reveal layers `aria-hidden`/`focusable="false"`/no `tabindex`/no interactive; chest controls are intentional, named, keyboard-activable; **no `.theme-*` bleed onto the gateway and no `:root` neutral edit** (R-M16-10). Veto on out-of-scope edits (map art, level pages, payload, audio engine). | security-engineer | G3, G4, G5 |
| G7 | **✅ DONE — QA GREEN (full suite 220 passed) — test re-point + new gateway tests (NF-M16-8, HARD GATE — largest regression surface).** *(as-built: the re-point also covered `tests/test_backend.py`'s `test_theme_default_horror_no_cookie`/`test_theme_sea_cookie_passed_to_context` (→ `/map/horror`/`/map/sea`) and the version-pin test (v8→v9); `tests/test_gateway.py` shipped with 30 tests.)* Execute the architect's CONCRETE migration map (ARCHITECTURE §17.7): **RE-POINT** the `/`-as-map assertions to the new subpage routes — `tests/test_frontend.py` (theme-class-from-cookie → route-forced theme at `/map/horror`/`/map/sea`; grid/hotspot tests → `/map/horror`; toggle-present/label → `/map/horror`+`/map/sea` + ADD a gateway test that `/` does NOT carry the map toggle; `test_index_links_all_static_assets` → SPLIT (subpages keep the map asset set; gateway asserts `style.css?v=9`+`theme.js`+`gateway.js`); keep `/` in the no-secret-leak parametrize + ADD `/map/horror`,`/map/sea`), `tests/test_horror_atmosphere.py` (swap the `/` entry in each `path` param to `/map/horror`, `/map/sea` for the sea-omit case; `/level/*` entries + the CSS-token tests reading `static/style.css` STAY), `tests/test_sea_landing.py` (sea-map cases → `/map/sea`, horror-map cases → `/map/horror`; the route now picks the arm, no cookie set). **LEAVE UNCHANGED:** all `/level/{id}` assertions, `test_m14_level_atmosphere.py` (21), `test_captions.py`, `test_cloudinary.py`, `test_backend.py` (`/api/*`), `test_sea_landing.py::test_level_routes_200_under_both_themes` + `test_api_levels_unchanged_by_m10`. **ADD NEW `tests/test_gateway.py`** (§17.7): `GET /`→200 renders `gateway.html`; neutral SSR / NO `theme-*` class even with a `theme=horror\|sea` cookie (parametrize both + none — D16/Q-M16-12); exactly 3 chests + moon/half/sun symbols (middle largest) + no visible chest text; each chest a focusable control with a non-empty `aria-label` + decorative reveal SVG `aria-hidden`/`focusable="false"`/no `tabindex`; cookie-before-navigate wiring (`Archive19Theme.choose(...)` + `location.assign("/map/horror"\|"/map/sea")` literals present); chest-2 `Math.random()` literal + both destinations reachable (D21); reduced-motion fallback path exists (CSS reduce rule + JS `matchMedia` branch); subpage deep-link standalone (`/map/horror`→`.theme-horror`+horror map, `/map/sea`→`.theme-sea`+sea map, no cookie — D15); `GET /` returns 200 not 3xx even with a stored cookie (D17/Q-M16-7). Verify `/level/{id}` (M14/M15/M11/M13) + audio engine unchanged; `prefers-reduced-motion` honored; responsive chest layout phone→PC; **full suite ends green (CI, HostR10).** | qa-tester | G6 |
| G8 | **✅ DONE — Completeness + docs + smoke** — full suite GREEN (220 passed); local `uvicorn` smoke PASSED (`/`→200 neutral `data-theme-neutral` with "choose a chest"; `/map/horror`→200 `.theme-horror`+`data-force-theme="horror"`; `/map/sea`→200 `.theme-sea`+`data-force-theme="sea"`; `GET /` with a `theme=sea` cookie still 200, not 3xx; `/level/1`→200; `/api/levels`→200); README + ARCHITECTURE §17 + REQUIREMENTS §17 + this PLAN synced (new routes, gateway flow, `theme.js` `choose()`/neutral-guard/forced-toggle, `static/gateway.js`, raster backdrop + D24 revision, gateway font/ambient, chest-2 rule, v9). Nothing committed (user controls commits). — full suite green; local `uvicorn` smoke (`/` gateway renders neutral with three chests, NO `.theme-*` class even with a cookie; chest selection writes the cookie + lands on the right map subpage with no flash; `GET /` is 200 not a redirect; both subpages SSR the right FORCED theme + deep-link standalone; the in-page toggle still flips on `/map/horror`/`/map/sea` and `/level/{id}` still honors the cookie — Q-M16-9/D23; `/level/{id}` both themes/states still 200); README + ARCHITECTURE §17 + REQUIREMENTS §17 + this PLAN synced (new routes, gateway flow, cookie-before-navigate, the `theme.js` `choose()` export, `static/gateway.js`, any new font/asset source/license, the chest-2 rule); completeness report. Nothing committed (user controls commits). | project-manager | G7 |

### Risk register (M16 — REQUIREMENTS R-M16-1…6 + the NEW architect risks R-M16-7…10 from ARCHITECTURE §17.8)
| # | Risk | Status / mitigation |
|---|---|---|
| R-M16-1 | **Test-suite regression from moving the maps off `/`** — the largest surface; `test_frontend.py`/`test_horror_atmosphere.py`/`test_sea_landing.py` assert map behavior at `/`. | **Planned + de-risked (NF-M16-8):** the architect produced a CONCRETE re-point map (§17.7) folded into G7 as an explicit task; full suite must end green (HostR10 CI gate). Route shape fixed first (Q-M16-4/D14). |
| R-M16-2 | **First route/SSR change since M13 touches `app/main.py`** — risk of breaking `read_theme`/SSR theme flow. | **Mitigated (D14/§17.2):** old `index()` → `gateway()` + two thin map handlers; `read_theme`/`_levels_payload`/`_local_audio_ids`/lifespan + every `/api/*` and `/level/*` handler are UNTOUCHED. SECURITY scope-audits (G6). |
| R-M16-3 | **Theme flash on the destination subpage** if the cookie isn't set before navigation. | **Mitigated (M16R9/D19):** `Archive19Theme.choose()` writes cookie+session synchronously BEFORE `location.assign`; belt-and-suspenders with D15's route-forced theme so SSR paints the right theme on first byte. QA verifies no flash/double-load (G7/G8). |
| R-M16-4 | **Reduced-motion / a11y miss** — a large zoom reveal or flashing without a reduce fallback, or text-free chests with no accessible name. | **Mitigated (NF-M16-2/D22/D24):** reduce-gated instant-resolve skip; `aria-label`s + keyboard activation on chest controls; SECURITY+QA a11y pass (G6/G7). |
| R-M16-5 | **Asset/dependency creep** — opting into Lottie/GIF adds a client dependency + bytes. | **Mitigated (Q-M16-3/D24/NF-M16-3):** default pure CSS+SVG; any raster/Lottie is budgeted + audited; no new secret (NF-M16-7). |
| R-M16-6 | **Gateway flash from a stale theme cookie** styling the neutral page. | **Mitigated (Q-M16-12/D16):** gateway ignores the cookie for its OWN render; theme-neutral scoped `.gateway` styling. QA parametrizes both cookies + none (G7). |
| R-M16-7 | **(NEW — architect) `theme.js` `reconcile()` regression** — teaching reconcile to trust the SSR-forced subpage theme over a stale cookie (D23) touches a function the toggle + every page depends on. | **CLOSED — no regression.** Built via the ADDITIVE `Archive19Theme.choose()` export + a `data-force-theme` arrival sync + a `data-theme-neutral` guard; `reconcile()` core left intact. QA confirmed (220 passed) the in-page toggle still flips in place on `/map/horror`/`/map/sea` (it now switches MAPS) and `/level/{id}` still honors the cookie; SECURITY approved the `theme.js` diff (G6). |
| R-M16-8 | **(NEW — architect) brand-logo `href="/"` now lands on the gateway, not a map** — `index.html`'s logo (line ~70) goes to the gateway after D14 (exactly Q-M16-10's lean). | **CLOSED — intended.** Accepted at G2 as the intended UX change ("home = your map" → "home = the chests"). The logo → `/` (gateway) shipped on the maps + level pages; no code risk, no regression. |
| R-M16-9 | **(NEW — architect) `animationend` never fires** — an interrupted/unsupported reveal animation could stall navigation. | **CLOSED — mitigated as designed (D24):** `static/gateway.js` uses the `hasNavigated`-guarded `animationend`+fallback-`setTimeout` handoff (exactly-once navigation); QA asserts the fallback branch exists. No stall observed. |
| R-M16-10 | **(NEW — architect) gateway has no `.theme-*` class → self-contained styling required** — shared rules keyed on `.theme-horror`/`.theme-sea` won't apply (intended — D16), but the gateway's neutral `.gateway` styling must NOT edit `:root` neutrals the maps/levels inherit. | **CLOSED — no bleed.** Gateway shipped theme-neutral (`data-theme-neutral`, scoped gateway styles, no `:root` edit); SECURITY+QA verified no `.theme-*` bleed and no `:root` regression (220 passed; both maps still SSR their forced theme). |

### Gate (M16)
**Phase 2 (Execution) COMPLETE — G3–G8 all DONE; BUILT + SECURITY-APPROVED + QA-VERIFIED (full suite 220 passed) + local `uvicorn` smoke PASSED — pending commit (2026-06-13).** Backend routes/SSR (G3), gateway template (G4), CSS + reveal/cookie JS (G5) shipped; SECURITY approved the scope (G6); QA executed the §17.7 test re-point + added `tests/test_gateway.py` (30 tests) with the full suite green (G7); PM ran the suite + live smoke and synced the docs (G8). New risks R-M16-7…10 are all CLOSED (register above). One as-built revision: D24's reveal art shipped as a painted raster backdrop (`static/img/gateway/gateway-bg.jpg`) rather than pure inline SVG (ARCHITECTURE/REQUIREMENTS §17 as-built notes). Nothing committed — the user controls commits.

**Phase 1 (Discovery) COMPLETE — G0 (capture) + G1 (architecture) + G2 (user sign-off) DONE; signed off 2026-06-13.** PM scaffolded requirements + milestone + open questions (G0); **SOLUTIONS_ARCHITECT** authored the full technical design (ARCHITECTURE §17, decisions **D14–D24**) — route topology (`/`=neutral gateway, maps→`/map/horror`+`/map/sea`, forced theme, no redirect), the template split (NEW `gateway.html` + reuse `index.html` byte-for-byte at the subpages), the cookie-before-navigate flow (additive `theme.js` `choose()`, NEW `gateway.js`), the chest-2 RANDOM rule, the pure-CSS+SVG reveal, the data-flow diagram, and the **concrete test-migration map (§17.7)**. **PM reconciled it against REQUIREMENTS §17 — no unmet requirement and no contradiction.** **USER signed off the 3 BLOCKING questions at G2:** Q-M16-1 = **RANDOM 50/50** (D21); Q-M16-4 = **`/map/horror` + `/map/sea`** (OVERRIDES the recommended bare `/horror`+`/sea`; `/` always the gateway, no redirect — D17); Q-M16-7 = **show every visit**. The 9 shaping Q's ride the architect defaults D14–D24; R-M16-8 (logo→gateway) accepted. New architect risks R-M16-7…10 are in the register above; R-M16-1 (the big test re-point) is de-risked into an explicit G7 task. **Next step: the TD design phase (`design-webpage` skill → `ui-designer`), then Phase 2 (G3 backend → G4/G5 frontend → G6 security → G7 QA → G8 PM verify, via `orchestrate-build`).** The map-tests-off-`/` re-point (NF-M16-8) is the biggest regression surface and is planned, not optional. CI must stay green (HostR10). A unit is done only when audited AND verified.
